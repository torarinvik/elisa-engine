# Elisa Engine — native implementation backlog

**Updated:** 2026-09-22. **Focus:** reusable Wicked + SDL3 backend services and their library integrations.
**Baseline:** inspect the current tree and linked validation notes before starting work.
This is the active execution plan. [Architecture](Elisa_Engine_Architecture_and_Plan.md)
defines the ownership model; [capabilities](docs/capabilities.md) records evidence.
Unfinished tasks below are proposals, not claims of existing engine support.

## Product objective and priorities

Build a reusable, authorable, shippable native game engine beyond the maze demo.
Deliver a persistent playable host, reusable subsystem services, a practical editor,
and packaged games using the same Elisa-owned world and public API.

- **P0:** remove foundations that currently prevent a reusable native engine.
- **P1:** deliver the common features needed to build and ship a substantial 3D game.
- **P2:** add depth, scale, multiplayer, production tooling, and broader platforms.
- **P3:** specialist capabilities, with a real example and a measured need.
- Near-term work prioritizes making the existing Wicked, SDL3, Jolt, miniaudio,
  cgltf, BasisU, meshoptimizer, ozz, Recast/Detour, and GameNetworkingSockets
  integrations into usable engine services. Prefer finishing these vertical slices
  over adding another dependency or a probe-only wrapper.
- **Active workstream:** finish the reusable native game runtime first. Do not move
  editor-shell, multiplayer, alternate-platform, or specialist-library breadth ahead
  of the backend sequence below unless a prerequisite or test failure requires it.
- A library integration is complete only when an ordinary Elisa application can use
  its public service, resource ownership and failure paths are tested, and a real
  sample consumes it. A native probe is supporting evidence, not the end product.
- Godot receives compatibility maintenance for changed shared contracts. New features
  may initially be native-only; report unsupported capabilities explicitly. Godot
  installation or renderer feature parity must not block the native development gate.
- Reuse the selected stack. Do not introduce another renderer, foreign gameplay ECS,
  high-level UI framework, or competing public scheduler to avoid engine work.
- Box2D, ACL, Steam Audio, FBX import, and richer authoring now have concrete tasks.
  They are no longer dismissed because the maze demo did not need them. Follow their
  prerequisites; do not fetch the entire specialist-library roadmap in advance.

## What exists, and what must not be mistaken for completion

The checked world, private owner fields, expression style, affine rejection fixtures,
maze gameplay, scene recorder, asset cooking, and implementation-linked proofs exist.
Preserve them. The current runtime executor is a **serial reference**, not a thread pool.
`native/wicked_probe.cpp` is a finite diagnostic host: it uses a canonical manifest,
synthetic input, and library probes. It is not a general game runner.
Jolt currently appears through Wicked's physics path; ozz, Recast/Detour, miniaudio,
Tracy, text, and GNS have integration evidence of varying depth, often probe-only.
A linked library, a policy enum, or one successful scene is not a public runtime service.
The existing source inventory is the starting point, not a reason to rewrite working code.

## Agent execution contract

1. Pick the first highest-priority task with completed prerequisites. Work through one
   coherent playable or testable slice; do not scaffold every wrapper before using one.
2. Read the relevant existing modules, ADRs, local library headers, and pinned source.
   Verify APIs and actual platform support there; this plan does not certify upstream features.
3. Elisa owns gameplay, world identity, scheduling policy, asset orchestration, animation
   behavior, navigation decisions, replication, editor models, and the public API.
   Keep C/C++/Objective-C shims narrow: opaque handles, plain ABI data, vendor operations.
4. Use last-expression returns, scoped loops with result bindings, and block expressions
   that contain temporary variables. Use `def Type() -> Type` constructors and named constants.
   Prefer suitable Elisa standard-library facilities over new engine utilities.
5. Use qualified module dependencies, small public surfaces, private owner fields, and
   phase-limited borrows. Keep vendor types out of public Elisa interfaces. Stay below
   600 lines per source/document file, including this plan; split by responsibility.
6. Preserve real error unions and failure atomicity. Define resource/thread/allocator
   ownership, callback lifetime, cancellation, capacity behavior, and destruction order.
7. A compiler, ElisaScript, or prover limitation blocking this style becomes a minimized
   regression and a fix in its owning repository. Do not flatten scopes or weaken checks
   as the permanent workaround; record the required toolchain revision.
8. Completion requires a public API used by real gameplay/editor code, positive and
   adversarial tests, native evidence where applicable, and documentation. Mocks validate
   contracts but cannot establish library or GPU integration. Tests must assert outcomes.
9. Record performance claims in optimized builds: warm-up policy, hardware, scene size,
   CPU/GPU time, p50/p95/p99, allocations, and peak memory. Qualifiers and block syntax
   should add no runtime machinery; verify generated code when that is the claim.
10. Change `[ ]` to `[x]` only with commit, command, result, artifact path, and limitations
    recorded in a linked `docs/validation/` note. Update capability labels precisely.
    Commit coherent changes. Do not mark a subsystem complete from its first smoke test.
11. If blocked, record the exact cause and a concrete prerequisite task, then continue
    another ready task. Hardware-unavailable checks remain unverified, never silently green.
    Product decisions such as the project's license do not block unrelated implementation.

## Delivery milestones

| Milestone | Exit evidence | Main task groups |
|---|---|---|
| M0 — reusable host | Visible interactive maze, proper close, resize, restart, reproducible native-only command; existing probes still pass | F01–F10, R01, I01 |
| M1 — reusable game runtime | Authored scene with multiple assets, streamed resources, PBR, collisions, save/load; no maze-specific host logic | W01–W06, A01–A06, R02–R05, R13, R15, P01–P04, S01–S02 |
| M2 — character playground | Animated controllable character, terrain/slopes, moving platforms, nav agents, spatial sound, debug overlays | C01–C08, N01–N04, P05–P08, S03–S04, R06–R09 |
| M3 — author and package | Editor creates/edits/saves a second game; standalone Release package runs outside the source tree | E01–E09, I02–I07, Q01–Q04, Q07 |
| M4 — scale and network | Measured crowd/streaming scene and two-process multiplayer game; soak tests and native CI evidence | W07–W10, N05–N06, T01–T08, Q05–Q09 |
| M5 — breadth | Tested opt-in advanced systems, each exercised by a shipped example | Remaining P2/P3 tasks |

**F08 is complete for the supported provider set. A04's production asynchronous
asset path is implemented and validated.** Continue with A05–A07, then the native
backend sequence below. Do not restart completed identity, field-privacy, render
extraction, or mesh/material API work. Milestones are outcome gates; individual
feature tasks may advance as soon as their explicit dependencies are ready.

## Current execution queue

Select the first ready item below unless new test evidence changes the order. Each item
points to the full acceptance criteria in its task entry; keep work vertical and leave
the public API connected to a real Elisa client.

1. **A05–A07 — complete cooked content import.** Connect glTF scene/material import,
   BasisU format selection, and meshoptimizer output to actual Wicked resources.
   Verify capacities, stale generations, cache invalidation, and packaged operation.
2. **R03–R07/R13/R15 — finish the core Wicked renderer service.** Connect camera and
   viewport lifetime, PBR material slots, lights/environment, quality negotiation,
   packaged shader permutations, and dependent render passes. Exercise resize,
   suspension, unsupported-feature fallbacks, and visual references in one authored
   scene; measure cold startup separately from steady-state frames.
3. **P01–P06 — make Jolt the gameplay physics service.** Elisa owns body and shape
   identity, fixed-step scheduling, interpolation, filtered queries, contact delivery,
   and character control. Prove one physics step per committed tick, no render-driven
   simulation, rollback on creation failure, and a playable obstacle-course sample.
4. **S01–S03/S05 — finish miniaudio as a game audio service.** Add generation-safe clips,
   streaming, buses, voice budgets, world attachment, spatial playback, and device-loss
   recovery. Test exhaustion, cancellation, fallback selection, and ordered shutdown in
   an ordinary game session.
5. **C01–C05/N01–N04 — turn ozz and Recast/Detour into gameplay services.** Cook
   skeleton/clip and multi-tile navigation assets, add bounded generation-checked runtime
   handles, then connect animation, IK, agent movement, and replanning to Elisa World.
   Demonstrate both in a controllable character sample with unload/reload coverage.
6. **I01–I07 — complete SDL3 input and Wicked UI/text services.** Use action maps for
   keyboard, mouse, and controller input; render interactive Elisa-owned UI; connect
   FreeType/HarfBuzz font shaping and IME text entry with focus/accessibility behavior.
   Validate high-DPI resize, device removal, and deterministic input delivery.
7. **W04/W05, R10/R11, and A10/A11 — scale the same backend vertically.** Stream scene
   cells and resource generations, preserve references through hot reload, and add
   visibility/LOD plus terrain/vegetation only with measured scene and memory budgets.
8. **Q01–Q04/Q07 — prove the engine can ship.** Add representative backend regression
   scenes, reproducible native CI/toolchain setup, useful crash diagnostics, a Release
   package that runs outside the checkout, and a second authored game using the public
   services. Move the editor shell E01–E09 forward after these runtime contracts exist.

Once this P1 native runtime lane is demonstrated in a packaged second game, advance the
P2 scale/network work and P3 specialists (including Box2D, ACL, Steam Audio, and XR)
according to their dependencies and measured consumers.

## F — native foundation and lifecycle

- [x] **F01 · P0 · Evidence and integration inventory** — After: none.
  Inspect `native/`, `src/backend/`, fetchers, CI, and the pinned Wicked fork; classify each service as policy, probe, reusable adapter, or game-integrated. Done: one inventory names entry points, owners, exact build identities, gaps, and commands; reconcile outdated completion claims without erasing historical evidence. Evidence: [`docs/native-integration-inventory.md`](docs/native-integration-inventory.md), committed as `f9cc344`; `wc -l docs/native-integration-inventory.md` (88) and `python3 scripts/check_source_length.py` passed. This inventory records the current finite-probe limitations; it does not claim F02–F10 completion.
- [x] **F02 · P0 · Persistent native application** — After: F01.
  Extract reusable startup/frame/shutdown code from `native/wicked_probe.cpp`; keep the probe as a client. Done: a visible maze runs until user close, pauses/resumes, restarts a world, and accepts real input without hardcoded frame counts or a manifest defining gameplay authority. Evidence: [`docs/validation/persistent-host.md`](docs/validation/persistent-host.md); `ELISA_PERSISTENT_HOST=1 ELISA_PERSISTENT_SELF_TEST=1 build/wicked-native-probe ... alwaysactive` passed with pause/resume, restart, movement, fixed ticks, close, and ordered shutdown. The generic `src/runtime/application.elisa` API and `scripts/elisa_build_run.py` now build an ordinary game-owned `main()` with no game C exports; its hidden-window lifecycle smoke passes on SDL3/Metal/Wicked. See [`docs/validation/application-lifecycle-abi.md`](docs/validation/application-lifecycle-abi.md). The self-test is deterministic; manual visual input remains a host-session check.
- [x] **F03 · P0 · Reproducible native dependency build** — After: F01.
  Lock Wicked, local patches, SDL3, Jolt, and currently used libraries/toolchains in a machine-readable manifest with checksums and build options. Done: a clean checkout can build using configured paths; stale artifacts are detected, and active targets neither fetch nor link obsolete SDL2. Evidence: `native/dependency-manifest.json` and `scripts/check_dependency_manifest.py` committed as `a6a7f9d`; `python3 scripts/check_dependency_manifest.py` passed against Wicked `21a1262b` after the SDL3-safe audio shutdown patch; the native driver preflight and full two-pass `elisascript scripts/wicked_probe.elisascript` passed using SDL3 and the recorded external artifacts. The manifest intentionally reports machine-specific paths through environment overrides; it does not vendor third-party source.
  Update: the Wicked pin now includes `14c15614`, which lets trails use hardware depth testing without the perspective depth-soften calculation or its zero-width division. The native gate rebuilds and validates against this exact revision; its run is recorded in [`render-scene validation`](docs/validation/elisa-render-scene.md).
- [x] **F04 · P0 · Versioned service ABI** — After: F02, F03.
  Define narrow C entry points and Elisa bindings for create/update/query/destroy, bounded spans, errors, allocator ownership, callback thread affinity, and feature/version negotiation. Done: ABI size/alignment tests and a real host client reject incompatible versions and malformed descriptors without exposing vendor structs.
  Evidence: [`docs/validation/service-abi.md`](docs/validation/service-abi.md), `python3 scripts/embed_probe.py` passed the generated Elisa archive through `ElisaServiceV1`, including create/update/query/destroy, version/size/allocator/buffer/stale-handle rejection, and ABI static assertions; committed with the implementation. F02 remains a separate persistent-host gate.
- [x] **F05 · P0 · Orderly startup and shutdown** — After: F04.
  Ensure the normal runner returns through ordered world teardown, callback drain, audio stop, GPU completion, and worker/device/window cleanup; patch pinned Wicked lifetime gaps. Done: partial-initialization failure and repeated scene restart leak no resources; normal process exit succeeds without forced termination.
  Progress: `NativeApplication` rolls back SDL/Wicked initialization failures at SDL, window, and Wicked stages. Shutdown closes callback admission, runs reverse-order hooks, drains Wicked jobs, then stops audio while SDL is live, waits for GPU work, destroys the Wicked application, clears the global device, destroys the window, and quits SDL. The shutdown-hook test submits a job and verifies it completes before device teardown. A fixed 64-entry function/context registry checks overflow rejection and reverse teardown order. Wicked commit `e45123b` destroys placement-constructed command lists on Metal, Vulkan, and DX12 and adds `WaitForAllJobs()`. Wicked commit `857d170` releases Metal shader dispatch data, the FAudio engine, and its owned reverb effect. Wicked commit `3a450df` adds worker-batch autorelease pools, scopes pre-main Apple resource-path lookups, and fixes Apple CMake helper/framework integration. Apple host operations also use scoped autorelease pools. The full native gate now runs 64 in-process scene restarts: six warm-up cycles and 58 measured cycles. Two native passes measured 0-byte GPU deltas and heap increases of 7,520 and 11,520 bytes. Eight host/device shutdown cycles measured heap increases of 555,840 and 4,512 bytes; the larger first pass includes a single ~540 KB step at cycle four that did not recur in the repeat pass. `OBJC_DEBUG_MISSING_POOLS=YES` still reports Apple AppIntents/LinkServices callbacks; LLDB confirms those stacks are outside Elisa and Wicked. The full SDL3/Wicked CMake build and two-pass native gate passed on the updated Mac toolchain. Evidence: [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md); F05 remains incomplete while scene heap growth and the first-pass host heap step need allocation attribution.
  Follow-up: `MallocStackLoggingNoCompact` snapshots pointed to cycle-7 Wicked pipeline-cache/Metal setup and 116 per-frame Lua temporary tables (6,496 bytes) across 58 measured restarts. Wicked commit `851c8f2` now avoids allocating the timer-wakeup queue when no timer is due; a Lua regression probe verifies 1,000 idle timer updates stay below 1 KiB heap growth and that due coroutines resume. The 512-cycle uninstrumented soak (16 warm-up, 496 measured) passed with a 0-byte GPU delta and 5,904-byte heap increase, down from 66,944 bytes before the fix. The full post-fix SDL3/Wicked two-pass gate also passed: exact frame determinism, 0-byte GPU delta and 1,824-byte heap delta over 58 restarts, and 22,064-byte heap delta over eight host cycles; the earlier ~540 KB step did not recur. Instrumented sizes remain invalid leak estimates. F05 stays partial: residual heap and churn deltas still need attribution. Commands and full results: [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Additional follow-up: a standalone restart-only run on macOS 27.0 showed a first-use 1,032,192-byte GPU-memory increase at cycle 7 when only six warm-up cycles were excluded. `malloc_history` showed Metal drawable activity, but attached during the warm-up hold and lacked earlier stacks, so its heap values are not used as leak evidence. The probe now defaults to 16 warm-up cycles; an uninstrumented 64-cycle rerun measured 0 GPU bytes and -3,600 process-heap bytes across 48 restarts. The independent host/device and post-churn deltas remain unattributed. See [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Latest two-pass SDL3/Wicked run after the capability-query refactor: each pass rendered and verified the frame, then ran 64 scene cycles with 16 warm-ups. GPU deltas were zero; heap deltas were 4,128 and 0 bytes. Both eight-cycle host lifecycles measured 3,504 bytes; post-churn steady-state deltas were 282,336 and 272,064 bytes. The earlier one-time host step did not recur. F05 stays partial pending attribution and another 512-cycle soak. Full output and command: [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Latest full-gate repeat after checked application startup, tangent-bearing FBX packages, and compact static-vertex indexing: exact rendered-frame comparison passed. Across two 64-cycle scene runs (16 warm-ups), GPU deltas were zero and process-heap deltas were 560 and -1,680 bytes; eight-cycle host lifecycles each measured 3,840 bytes, and post-churn steady-state deltas were 282,304 bytes in both passes. Host/churn heap changes remain unattributed, so F05 stays partial. Evidence: [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Additional attribution: Wicked commit `07aac68` adds `wi::lua::ShutdownApplication()`, which cancels Lua processes, clears the `main` and `application` globals only when they still wrap the retiring app, then collects unreachable wrappers before audio/device teardown. The lifecycle gate asserts both globals are nil after every host shutdown. A same-process macOS 27.0 malloc-history diff found 14 Lua `Application_BindLua` allocations (896 bytes) accumulating before the fix; after it, no such wrappers remained and only two 112-byte `l_alloc` blocks were added between cycles 1 and 8. The instrumented 23 KiB total delta is mostly AppKit/CoreFoundation allocations and is not a leak estimate. The uninstrumented eight-cycle host delta was 4,656 bytes; F05 remains partial pending the remaining host/churn attribution and a repeat long soak. See [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Latest full `native` gate after the Lua cleanup passed the actual Metal render, exact frame comparison, live-input probe, partial-startup rollback, and repeated shutdown. The frame and rerun host lifecycles measured 4,656 and 13,184 bytes; 64-cycle scene restarts changed heap by -1,696 and 3,072 bytes with zero GPU delta; post-churn steady-state growth was 282,304 and 284,896 bytes. The Lua wrapper accumulation is fixed, but host/churn residuals remain unattributed, so F05 stays partial. The full Elisa suite, including Godot and proof checks, also passed. See [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Latest native-gate follow-up traced the apparent ~280 KB post-churn increase to GPU mesh suballocations measured before Wicked's frame-aged retirement ran. The probe now advances `GetBufferCount()+1` normal frames and waits for GPU completion at the two heap checkpoints, excluding those retirement frames from per-batch churn timing. The two native frame passes then measured settled churn deltas of -100,896 and -93,024 bytes. The full gate passed exact frame comparison and live input; scene restart deltas were 3,824 and 96 heap bytes with zero GPU change, and eight-cycle host deltas were 6,704 and 11,136 bytes. A fresh 512-cycle soak (16 warm-up, 496 measured) passed with 0 GPU delta and 6,512 heap bytes. Churn growth is resolved as a measurement-timing issue; the small host-cycle heap delta remains under attribution, so F05 stays partial. See [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Latest full Elisa suite and native gate after the SDL3 input and native module-split changes: the full suite passed, including Godot 4.7.2 and both proof suites; two native passes passed exact frame comparison, live input, and orderly shutdown. Scene restarts measured -272 heap bytes and 0 GPU bytes in both runs; post-retirement churn deltas were -90,272 and -97,088 bytes. Host lifecycle heap deltas were 4,656 and 16,608 bytes, so F05 remains partial pending attribution. The source-length and module-hygiene gates passed. See [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
  Host-memory attribution: the lifecycle probe primes its global-handle check and runs a second Lua collection before sampling, so finalizable app userdata and probe-interned keys do not distort the baseline. A macOS 27.0 cycle-1/cycle-8 heap diff found 112 new live allocations (~31 KB), mostly Apple frameworks, zero new leak nodes, no remaining `Application_BindLua` userdata, and two fixed 64-byte Lua tables recreated by Wicked's `killProcesses()`. A matching uninstrumented eight-cycle run ended 10,096 bytes below its steady sample. Completion: the corrected two-pass native gate passed frame determinism, live input, startup rollback, and orderly shutdown; host deltas were 176 and 3,808 bytes. Two 512-cycle soaks (16 warm-ups, 496 measured) had zero GPU delta and heap deltas of 6,512 and 4,976 bytes. The remaining small process-heap variation is bounded across both long soaks and attributed to runtime/framework state; no application-wrapper accumulation or new leak nodes remain. F05 is complete. Details: [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
- [x] **F05b · P1 · Host re-initialisation under a full scene** — After: F05.
  Progress: a missing default audio device now degrades the start to the silent miniaudio provider at requirement planning (the decision is rewritten to `MiniaudioSilent` and `open` reports `FallbackRequired`), and a device that probes fine but fails to open is activated silently in place, so neither case rolls the host back. The restart probe exercises a bounded authored full scene on every in-process host cycle: 40 Wicked cube objects arranged as a grid, plus camera and light, two rendered frames, GPU completion, and component-count cleanup. On macOS 27.0, focused three-cycle and rebuilt 16-cycle runs passed with `rendered=1 components_cleared=1`; the rebuilt run reported `gpu_delta_bytes=0` and `heap_delta_bytes=4272`. The authored Amazing Labyrinth smoke now requests the real Miniaudio fallback, loads and renders its cooked mesh and texture bundles, shuts down, and repeats the complete host lifecycle in the same process. With `ELISA_AUDIO_FORCE_DEVICE_UNAVAILABLE=1`, the two-cycle SDL3/Metal run exited 0, proving the imported full scene no longer hangs during reinitialization. Evidence: [`docs/validation/runtime-service-rollback.md`](docs/validation/runtime-service-rollback.md) and [`docs/validation/orderly-shutdown.md`](docs/validation/orderly-shutdown.md).
- [x] **F06 · P0 · Native resource handles and retirement** — After: F04.
  Generalize generation and submission-lifetime contracts to real mesh, material, texture, body, and voice handles; separate logical destruction from fence-delayed release. Done: stale and cross-world handles, generation exhaustion, failed creation, bounded-capacity rejection, and destroy-while-in-flight are exercised against real resources. Evidence: [`docs/validation/native-resource-retirement.md`](docs/validation/native-resource-retirement.md); the two-pass Wicked native gate passed pool saturation, queue saturation/recovery, voice cleanup on generation exhaustion, deferred texture/voice release, stale-handle rejection, and object-baseline checks. Production imported-mesh upload and material binding remain tracked under A05, R02, and R04.
- [x] **F07 · P0 · Coordinate and numeric conventions** — After: F04.
  Centralize units, handedness, matrix order, quaternion layout, depth, winding, tangent parity, and camera conversion instead of probe-local sign flips. Done: round trips and an asymmetric scene validate render, physics, skinning, ray picking, and negative/nonuniform scale conventions. `native/coordinate_abi.h` v2 validates/converts column-major affine matrices, normalized XYZW quaternions, and signed tangent-frame parity; `native/coordinate_transform_bridge.h` submits TRS/matrix payloads to Wicked with explicit units and row-vector conversion, rejecting shear. Elisa and native tests apply the same basis-reflection and negative-scale rule. Wicked commit `a149914` stores world-transform orientation in existing `ShaderMeshInstance` padding and applies it to raster and ray-tracing tangent frames; the normal-mapped asymmetric signed-scale render compares exact rerun pixels against [`backends/coordinate_reference.png`](backends/coordinate_reference.png). Picking verifies a center hit and a miss beyond a negatively/nonuniformly scaled cube. A dynamic signed-scale box is stepped and queried against Jolt; Wicked commit `bf8945b` uses scale magnitudes for primitive collision dimensions. The skin fixture uploads Elisa-deformed vertices, applies a signed nonuniform transform, uses shared winding parity, and checks all four resulting Wicked world positions. Evidence: [`docs/validation/coordinate-conventions.md`](docs/validation/coordinate-conventions.md); `scripts/check.elisascript` and the two-pass `scripts/wicked_probe.elisascript` passed. Follow-up: the render-scene service now crosses the same boundary for transforms, the camera, cooked vertices and tangents, poses, the sun and arcs, so its frames show +X on the right, as Godot's do. Evidence: [`docs/validation/render-scene-handedness.md`](docs/validation/render-scene-handedness.md). The Godot host now reverses cooked winding once at its upload boundary, since Godot draws clockwise front faces, and the maze tile fixture no longer winds six triangles inward; headless and rendered probes check both. Evidence: [`docs/validation/godot-cooked-winding.md`](docs/validation/godot-cooked-winding.md).
- [x] **F08 · P0 · Runtime capability negotiation** — After: F04.
  Replace coarse profile assumptions with queried service/format/limit/feature support and explicit fallback policy. Done: unavailable renderer features and missing optional libraries yield actionable errors or tested fallbacks; a dependency's advertised feature cannot automatically mark the engine capability supported. Progress: Elisa resolves bounded feature and limit requirements into `Ready`, `Fallback`, `Unavailable`, or `Invalid`; `negotiate_limit` includes remaining memory, and typed texture negotiation matches the native BC1/normal-map/alpha fallback rules. `negotiate_requirements` takes a typed provider map, reports the selected fallback identity for each request, distinguishes `FallbackDisallowed` from a missing provider, and rejects provider/service mismatches. `test/capabilities.elisa` covers all eight routes, mixed results, provider identity, mismatch rejection, and native routes with no spurious fallback provider. ABI v2 in `native/capability_abi.h` carries optional graphics bits, queried RGBA8/BC1/R16F support, memory budget/usage, and actual Wicked high-priority/streaming worker counts. `maze_backend_configure` maps the validated SDL3/Wicked host report to the live Elisa profile before game startup; removing Input rejects start, and malformed reconfiguration preserves the last valid profile. Unknown resource capacities remain zero and cannot satisfy requirements. Typed C queries validate individual features, formats, and viewport/worker/memory limits; unknown bits and inconsistent profiles are rejected. Evidence: [`docs/validation/capability-negotiation.md`](docs/validation/capability-negotiation.md), the full Elisa suite, and the two-pass native gate. Startup now probes Jolt and miniaudio adapters through temporary initialization and teardown; a failed probe reports `ProviderUnavailable` and rolls back startup. The live profile still does not advertise Physics or Audio as core services; the owning RuntimeServices session activates explicitly requested fallbacks. Only Physics and Audio adapters exist today.
  Progress: `Application::backend_profile()` exposes the shared validated SDL3/Wicked startup report to ordinary Elisa applications, with an error union for stopped hosts, wrong-thread calls, and unavailable or invalid reports. `Application::initialize_with_requirements()` negotiates bounded required-service sets before gameplay; it reports declared fallbacks explicitly and rolls back startup on invalid or unavailable requirements. Both the maze host and normal applications use one native device-query helper. The live profile still reports Physics and Audio as unavailable. `src/backend/requirements.elisa` aggregates service, optional-feature, limit, and texture requirements under fixed capacities; it returns selected limit values and per-texture encoding decisions, and combines results with Invalid > Unavailable > Fallback > Ready precedence. `Application::initialize_with_profile_requirements()` validates that contract before gameplay and rolls the host back on invalid/unavailable requests. The portable suite covers mixed outcomes, unknown limits, explicit optional/texture fallbacks, malformed ranges, and capacity overflow. `src/audio/runtime.elisa` provides an opt-in miniaudio adapter for callers that negotiate Audio fallback: initialize a silent or default device, decode a WAV, play/stop generation-checked voices, set bus gain, and shut down with the application. Opaque handles and a generated-WAV smoke verify decode, playback state, and shutdown. `src/physics/runtime.elisa` provides an opt-in Jolt-backed Physics fallback in a separate scene; the ordinary hidden SDL3/Metal application smoke verifies body creation/destruction, that a destroyed handle maps to `InvalidHandle`, eight fixed steps and falling motion, committed ticks, host-shutdown invalidation, and the Audio fallback across repeated host startup/shutdown. Stage1 previously resolved sibling nested `Status` modules by the first matching short owner; the correction and stage0/stage1 regression are in Elisa-compiler. `WorldPhysics` binds checked gameplay entities to session-owned bodies and dispatches the validated Physics -> WorldSync system plan through the generic executor for every due fixed tick, including affine runtime contexts. The compiler regression and native SDL3/Metal application smoke pass. Spatial audio integration remains. Evidence: [`docs/validation/capability-negotiation.md`](docs/validation/capability-negotiation.md) and [`docs/validation/audio-runtime.md`](docs/validation/audio-runtime.md).
  Progress: the SDL3/Metal application smoke also requests BC1 for a normal map and verifies `FallbackRequired`, an RGBA8 texture decision, and that the host remains initialized until the caller applies the fallback and shuts down. Native smoke and full report: [`docs/validation/capability-negotiation.md`](docs/validation/capability-negotiation.md).
  Progress: service requirement reports now return ordered, bounded decisions for every requested feature (`Native`, `DeclaredFallback`, `ProviderUnavailable`, or `Unavailable`), so mixed requests identify exactly which handlers the caller must run. Portable tests cover all eight features and mixed outcomes; engine commit `cbb5f39` passed the post-commit SDL3/Metal application smoke for Physics and Audio decisions and fallback lifecycles. Evidence: [`docs/validation/capability-negotiation.md`](docs/validation/capability-negotiation.md). Provider selection remains caller-owned; only Physics and Audio have adapters today.
  Progress: `test/physics_app_probe.elisa` is now called by the native application smoke. It runs two fallible Jolt fixed steps and returns `committed_tick` as the function's last expression through an error union; the full SDL3/Metal application and failure-cleanup smoke passed.
  Progress: startup probes Jolt scene creation and miniaudio silent/default device initialization, then tears each temporary adapter down before reporting its route as available. A failed probe changes that decision to `ProviderUnavailable`, recalculates ordered report counts, and rolls back service-only and aggregate profile startup. Fault injection verifies Physics scene and miniaudio device partial-init cleanup, rejected startup, and successful retries; synchronous upload and asset-loading fallbacks remain unavailable. Completion evidence: [`docs/validation/capability-negotiation.md`](docs/validation/capability-negotiation.md) records the 2026-09-21 provider matrix, full Elisa/Elisa Proof suite, SDL3/Metal lifecycle and rollback smokes, module/source/dependency policies, and unavailable-provider limits.
  Progress: `src/runtime/world_physics.elisa` exposes a bounded affine entity/body binding set in the public runtime bundle. It creates Jolt boxes at checked `World` transforms, advances each due fixed tick through the owning session, stages every body pose before publishing a synchronization pass, preserves entity rotation/scale, and supports retry-safe explicit unbinding. `WorldPhysics` also binds validated entities to current hierarchy poses and stages hierarchy synchronization parent-first, independent of binding order; `advance_and_sync_hierarchy` shares the owning session clock and runs the complete path per due tick. `test/runtime_services_probe.elisa` exercises duplicate-link rejection, gravity-driven flat and hierarchy pose publication, scale preservation, recomposition with an unbound intermediary, per-tick interpolation history, parent/child world-pose stability, handle-table compaction after removing one of two bodies, and cleanup through the native SDL3/Metal application smoke. The same probe builds an automatic Physics -> WorldSync plan, dispatches it on every due tick, and verifies execution order and transform updates. `Executor::plan_execute_with_context` and `plan_execute_with_contexts` provide allocation-free typed callback dispatch, covered by portable order/state tests. The native probe now uses `plan_execute_with_contexts` for affine `TickContext` callbacks. Elisa-compiler first resolves module-owned function values, then preserves the exact module owner for qualified fallible generic specialization; the regression covers two affine contexts, private callback dispatch, the schedule loop, and the fallible qualified call. The focused stage0/stage1 compiler regression and the real SDL3/Metal application smoke passed. Additional provider routes and spatial audio remain open.
  Progress: the SDL3/Metal application smoke injects failures after Jolt scene allocation and miniaudio device startup, proving cleanup, honest reports, and successful typed-provider retries. The hooks compile only under `--native-test-probes`; Audio sample-rate and channel bounds are named in Elisa and the native ABI. `src/runtime/services.elisa` adds an affine `RuntimeServices::Session` that owns Application plus report-selected Jolt/miniaudio fallbacks, activates adapters after host initialization, rolls back a partial Physics+Audio startup, rejects a repeated open without disturbing the live session, exposes owner-checked Physics operations, and shuts down Audio, Physics, then Application. Its native smoke covers failure rollback, retry, a two-tick fixed-step batch through Jolt, body query, session-routed audio playback and stop, missing-Audio behavior, and shutdown. Engine commit `6e49ed6` passed the real SDL3/Metal native gate and full `scripts/check.elisascript` suite on macOS 27.0 / Apple M5; details are in [`service rollback validation`](docs/validation/runtime-service-rollback.md). The live profile still advertises Physics and Audio only as declared fallbacks, not native core services. Device recovery/spatial audio and additional provider routes remain partial.
  Progress: the ordinary maze client now opens and shuts down through `RuntimeServices::Session`, and routes pump, frame snapshots, and exit requests through that owner. Engine commit `bf592f8` passed the SDL3/Metal application-service probe and hidden maze smoke; the probe also verifies these host calls reject use after shutdown. Native generic plan dispatch is now covered by the affine-context compiler regression and application smoke; hierarchy-aware physics synchronization is covered by the F08 integration probe.
  Progress: `RuntimeServices::Session::open` now preserves valid `CallerDefined` fallback decisions and returns `FallbackRequired` without trying to activate an external handler. The native service probe verifies the route identity, confirms engine-owned Physics/Audio remain inactive, and closes the session cleanly. Caller-owned handlers remain the application's responsibility.
  Progress: miniaudio's device notification callback now publishes a bounded atomic recovery request; `RuntimeServices::pump` consumes it on the owner thread and reopens the service on the silent backend. Recovery preserves decoded clips, invalidates active voices, and updates the session's typed provider; failed reopen marks Audio inactive and reports `AudioRecoveryFailed`. The SDL3/Metal application smoke drives the real callback with a synthetic stopped notification and verifies route change, stale-voice rejection, clip replay, and cleanup. Other provider routes, spatial audio, streaming, and live profile advertisement remain open. See [`docs/validation/audio-runtime.md`](docs/validation/audio-runtime.md).
  Progress: `src/runtime/world_audio.elisa` connects the session's Audio route to the World. An affine `WorldAudio::Emitters` set binds checked entities to session-owned voices. Each update derives listener and source velocity from committed transforms, with a 0.25 s hitch reset, and pushes `SpatialAudio` gain and Doppler pitch through the new validated `elisa_audio_v1_set_voice_spatial` ABI. Despawned entities and ended voices are detached safely. The SDL3/Metal application smoke runs the adversarial `test/world_audio_probe.elisa`. Other provider routes and live profile advertisement remain open. See [`docs/validation/spatial-audio.md`](docs/validation/spatial-audio.md).
- [x] **F09 · P0 · SDL3 lifecycle and frame pacing** — After: F02, F07.
  Handle focus, high-DPI resize, minimize, fullscreen, display changes, close, and suspended simulation; separate fixed ticks from presentation. Done: zero-sized windows and variable render cadence do not corrupt simulation, busy-spin, stretch viewports, or lose input transitions. `NativeApplication::WindowState` suspends on focus loss, minimize, or zero pixel extent; `run_frame()` skips presentation while suspended, and `advance_fixed()` clears backlog. Backing size and DPI come from Wicked's native window properties to preserve Cocoa Retina drawable dimensions; window metric changes recreate the existing Metal swapchain and refresh the canvas before presentation. `native/input_tick_queue.h` keeps movement and restart events in bounded FIFO order until fixed ticks consume them; overflow is explicit. Evidence: [`docs/validation/sdl3-lifecycle.md`](docs/validation/sdl3-lifecycle.md); the two-pass native gate passed synthetic display-change, actual SDL resize/fullscreen/backbuffer checks, and fixed-tick input tests; the persistent-host self-test passed pause/resume, restart, movement, and close.
- [x] **F10 · P0 · Native-first validation command** — After: F03, F05, F08, F09.
  Add composable ElisaScript quick/headless/native-GPU checks over existing gates, with structured evidence and provenance. Done: the native gate runs without Godot installed, invalidates stale success, distinguishes skip from pass, and retains shared-contract and optional Godot regression commands. `scripts/native_gate.elisascript` provides quick/headless/native modes, clears stale reports, uses explicit pass/fail/skip stage semantics, and keeps Godot out of the native prerequisites. All repository checks resolve from the gate's script path, independent of the caller's working directory. Native stages stream output and preserve their exit status; the schema-2 report records dependency, source, module, headless, application, and Wicked states plus checkout/toolchain provenance. The native gate compiles and runs Elisa `main()` clients through the generic build/run command, including application lifecycle, PhysicsRuntime, Audio fallback, and real render-scene smokes. A deliberate early assertion return verifies that the outer entry shuts down SDL3/Wicked and invalidates the backend profile. The sanitizer boundary harness links `native/miniaudio_implementation.cpp`. Validation: `DEVELOPER_DIR=/Library/Developer/CommandLineTools ELISA_COMPILER_BIN="../Elisa-compiler/scripts/elisac_stage1.sh" elisascript scripts/native_gate.elisascript native` passed on macOS 27.0/Apple M5; all report stages passed and `hardware_verification=verified`. Report: [`build/native-gate.json`](build/native-gate.json); implementation commit: `3a2f4dd`; details: [`docs/validation/native-gate.md`](docs/validation/native-gate.md). Cross-machine dependency/toolchain bootstrap remains Q03.

## W — world, scenes, persistence, and execution

- [ ] **W01 · P1 · General world storage beyond the maze** — After: F04.
  Extend the checked world with registered engine/game types and measured dynamic storage, preserving algebraic modeling and branded identity. Done: two unrelated game entity sets coexist; growth failure, compaction, and destruction preserve invariants without maze-specific actor limits or a replacement foreign ECS.
  Progress: `src/world/storage.elisa` adds an affine typed catalogue with separate actor/projectile columns, one owned monotonic ID stream, typed lookup, destruction, and independent compaction; `test/world_storage.elisa` now fills the 128-row actor column, rejects growth beyond capacity, destroys a middle row, and verifies compaction plus monotonic replacement IDs in `scripts/check.elisascript`. The primary `World` registry stores validated transforms for actors, enemies and bounded `StaticObject.Structure` entities. Static-object allocation, lookup, pose updates, despawn, compaction and rollback are covered by `test/world.elisa`; render snapshots compose the world pose with per-render local attachment transforms. Broader game-defined type registration, dynamic storage and timed measurements remain.
  Progress: the same catalogue now accepts up to 16 game-defined type registrations with per-type row capacities and fixed schemas of one to eight `i64` fields, stores up to 256 dense dynamic records, and exposes checked type capacity/field-count, lookup, schema-checked spawn, validation, and destruction operations. Dynamic rows share the catalogue's monotonic identity stream and compact in place after destruction; rejected duplicate/invalid registrations, field-count mismatches, per-type capacity, global capacity, stale lookup and replacement IDs are covered by `test/world_storage.elisa`. `test/world_storage_benchmark.elisa` and `scripts/world_storage_benchmark.py` provide a repeatable 64-round insertion/destruction/replacement workload; nine fresh-process runs measured a 23.784 ms median and 80.297 ms p95 on macOS 27.0 / Apple M5 (details: [`docs/validation/world-storage.md`](docs/validation/world-storage.md)). Typed engine columns remain allocation-free; richer typed payloads beyond bounded scalar fields remain.
- [x] **W02 · P1 · Transform hierarchy** — After: W01, F07.
  Add local/world transforms, parenting, dirty propagation, interpolation snapshots, and checked reparenting policy. Done: cycle rejection, keep-world/keep-local behavior, deep trees, parent deletion, and physics-owned transforms agree with rendered positions.
  Progress: `src/world/hierarchy.elisa` provides bounded 256-node local/world TRS nodes, dirty propagation, cycle and missing-parent rejection, keep-world reparenting with full TRS inversion, root detachment on parent removal, interpolation snapshots, teleport bypass, and an explicit physics-owned world-pose publication path. `World::World` now owns the hierarchy: spawn/despawn, world-space transform updates, checked reparenting, registry synchronization, validity checks, and rollback snapshots preserve one authoritative tree. `test/hierarchy.elisa` and `test/world.elisa` cover those behaviors plus a 31-node deep-chain propagation stress case and primary-world integration in the shared gate. `test/hierarchy_benchmark.elisa` and `scripts/hierarchy_benchmark.py` measured a full 256-node chain with eight root updates per round at a 582.427 ms median and 607.918 ms p95 across nine fresh processes on macOS 27.0 / Apple M5. Applying deferred commands directly to `World` remains W03.
- [ ] **W03 · P1 · Deferred structural mutation** — After: W01.
  Implement phase-bound spawn/despawn/reparent command buffers with stable ordering and an explicit atomicity policy. Done: iteration borrows cannot outlive the phase; conflicting commands, allocation failure, and despawn during events cause neither partial entities nor dangling borrows.
  Progress: `src/world/commands.elisa` provides a bounded affine spawn/despawn/reparent buffer with insertion order, atomic duplicate-write rejection, and capacity-aware commit that preserves the batch on allocation failure; `test/world_commands.elisa` covers those contracts in the shared gate. Applying accepted commands to `World` and borrow lifetime diagnostics remain.
  Progress: accepted command batches now apply atomically to `WorldHierarchy`. The hierarchy exposes a bounded structural snapshot/restore boundary, so a missing parent, duplicate spawn, invalid despawn, or cycle restores every node and leaves the command buffer pending for retry. `test/world_commands.elisa` covers spawn, local-pose preservation across reparent, despawn, rejection rollback, and pending-batch retention. Applying commands directly to the primary `World` registry and compiler diagnostics for phase-borrow lifetimes remain.
  Progress: `WorldCommands::WorldBuffer` now applies spawn, despawn and reparent batches to the primary `World` registry. Read-only world preflight rejects wrong-world references and cycles before earlier commands can mutate the batch; accepted batches clear the affine buffer, rejected batches retain it, and the rollback snapshot covers unexpected apply failures. `src/world/access.elisa` adds an affine six-phase access frame with read/write conflict checks, exclusive structural tokens, input/render mutation rejection, and no phase advance or frame end while a token is live. `src/world/phase_commands.elisa` acquires that token around primary-world commits; `test/world_command_primary.elisa` and the phase section of `test/world_commands.elisa` cover transform-preserving reparent, cycle rejection, static spawn, wrong-epoch despawn, valid despawn, mixed-batch atomicity, and phase-bound mutation. Compiler phase-borrow lifetime diagnostics and event scheduling integration remain.
- [ ] **W04 · P1 · Scene and prefab instances** — After: W02, W03, A01.
  Define versioned scene/prefab data, stable authoring IDs, runtime remapping, nested instances, and overrides. Done: the same prefab can spawn twice without identity collisions; save/reload preserves overrides and rejects cycles or missing references with useful diagnostics.
  Progress: `src/world/prefab.elisa` defines versioned bounded nodes, stable authoring IDs, per-instance runtime remapping, transform overrides, and affine instance validation; `test/prefab.elisa` covers two instances, missing parents, cycles, and override isolation. Nested instances, serialized overrides, and primary-world integration remain.
  Progress: `src/world/prefab_world.elisa` now maps each definition into the primary `World` through private runtime bindings and deferred structural commands; `test/prefab_world.elisa` covers two live instances, world-space hierarchy, override isolation, destruction, and reload from a stable-ID transform snapshot. `src/world/prefab_persistence.elisa` keeps native references out of the snapshot. `src/world/prefab_scene.elisa` adds bounded nested links with topological parent validation, placement, reverse-order destruction, and cycle/missing-definition rejection; `test/prefab_scene.elisa` exercises the nested world result. `scripts/scene_file.py` now defines a canonical version-1 scene document with bounded definitions, links, overrides, finite transforms, stable authoring IDs, parent topology checks, version-0 migration, and explicit rejection of native handles; it persists through the fsynced save journal and self-tests round trips, migration, cycles, overrides, and malformed runtime data. Native world epochs and resource rehydration remain.
- [ ] **W05 · P1 · Streaming world cells** — After: W04, A04, F06.
  Extend bounded maze streaming into asynchronous scene-cell activation with dependencies, hysteresis, cancellation, and unload policy. Done: a player crosses cell boundaries while referenced resources remain valid; repeated travel stays within declared CPU/GPU memory budgets.
  Progress: `src/world/cell_streaming.elisa` adds bounded generation-checked cell requests, dependency-generation validation, resident budgets, cancellation, and hysteretic trimming; `test/cell_streaming.elisa` covers coalescing, stale dependencies, budget rejection, unload, and reactivation. Native decode/upload scheduling and primary-world cell activation remain.
  Progress: `src/world/cell_world.elisa` now owns a generation-checked primary-world entity per resident cell, rolls back failed activation, releases entities before slot reuse, and trims with the configured hysteresis. `test/cell_world.elisa` covers coalescing, budget rejection, world identity, trimming, generation reuse, and teardown. Native asset decode/upload scheduling and real scene-cell payload activation remain.
- [ ] **W06 · P1 · Savegame schema and migrations** — After: W04.
  Serialize stable data and IDs, excluding native handles; add transactional writes, checksums, migration chains, and crash recovery. Done: old-version, truncated, oversized, and corrupt saves are tested; loading replaces the world atomically and reconstructs native state.
  Progress: `src/world/save_schema.elisa` adds stable-ID records, deterministic checksums, a bounded version-1-to-2 migration, and affine staged commit/abort; `test/save_schema.elisa` covers commit, migration, checksum validation, and abort isolation. `scripts/save_journal.py` now provides fsynced canonical payloads, journaled atomic replacement, interrupted-write recovery, corrupt-journal fallback, and explicit malformed/truncated-payload diagnostics in the shared gate. `World::Rollback::replace` now stages the current complete runtime snapshot before applying a replacement, and `test/world.elisa` proves an invalid replacement leaves the world unchanged. Cross-epoch durable reconstruction and native resource rehydration remain.
- [ ] **W07 · P2 · Real parallel executor** — After: W03, F05.
  Keep `src/runtime/executor.elisa` as the serial oracle; execute proven-independent work on a reusable worker pool using Elisa concurrency facilities where suitable. Done: cancellation, shutdown, dependencies, and effect/resource conflicts are tested; parallel results match the serial reference for deterministic systems.
  Progress: `native/parallel_executor.h` now executes bounded approved waves concurrently with a custom barrier, joins each wave before the next, caps workers, propagates task exceptions, and supports cooperative cancellation between joined waves; the Wicked gate proves overlap, wave ordering, cap rejection, failure handling, and cancellation. `src/runtime/schedule.elisa` now assigns dependency-preserving waves, and `Executor::plan_valid` rejects hand-built groups that reverse a conflict; `test/schedule.elisa` covers transitive dependencies plus legal reordering of independent work. Elisa schedule-to-native dispatch and serial trace equivalence remain.
- [ ] **W08 · P2 · World events and service phases** — After: W03, F04.
  Define bounded typed event queues and an explicit input/simulation/physics/animation/render/audio order, including overflow and unsubscribe behavior. Done: reentrant callbacks, listener destruction, and worker-to-main delivery cannot mutate a world during an invalid access phase.
  Progress: `src/world/events.elisa` adds bounded typed events carrying branded `World::EntityRef` values, explicit six-phase ordering, listener subscriptions, unsubscription, phase-gated emission, single-delivery drains, and `emit_world`, which validates the reference against the primary world before queueing. `test/world_events.elisa` covers phase regression, listener removal, and rejection of despawned and wrong-epoch entities. `native/world_event_bridge.h` adds a mutex-protected worker-to-main handoff with fixed storage, phase guards, one-shot delivery, unsubscribe suppression, and a reentrant dispatch/phase guard covered by the Wicked probe; world-borrow integration remains.
- [ ] **W09 · P2 · Replay and state diagnostics** — After: W06, W08.
  Record tick-stamped input, random seeds, decisions, and state digests with scoped determinism guarantees. Done: replay finds the first divergent subsystem/tick; floating-point and solver/build boundaries are declared instead of promising cross-platform bit identity.
  Progress: `src/runtime/replay.elisa` records bounded tick/input/seed/world/physics/render frames, declares same-build versus cross-build scope, reports the first divergent tick, encodes one frame as a fixed little-endian 48-byte payload, and exposes a bounded deterministic scalar `Digest` accumulator for subsystem state. `native/replay_trace.h` persists up to 256 frames through an atomic temporary-file replacement with explicit version, scope, count, and FNV-1a checksum validation; malformed, truncated, trailing, corrupt, and non-monotonic traces are rejected before publication. `test/replay.elisa` and the native probe cover matching, divergence, duplicate ticks, frame round trips, digest changes, and corruption rejection. Wiring concrete world, physics, and render snapshots into the accumulator remains.
- [ ] **W10 · P2 · Storage scale and world proofs** — After: W03, W07.
  Benchmark hot iteration, spawn/despawn, lookup, and compaction; improve measured layouts and grow implementation-linked proofs where supported. Done: representative small/large worlds show before/after timings and memory, and proof claims name exact invariants rather than claiming whole-engine safety.

## A — assets, cooking, loading, and content pipelines

- [x] **A01 · P0 · Stable asset identity and schema** — After: F01.
  Extend descriptors with distinct source identity, content hash, artifact variant, schema version, dependencies, and import settings. Done: renames preserve references; changing content/settings/tool versions invalidates exactly the affected artifacts; IDs are not process hashes or filesystem paths. Evidence: `Assets::AssetDescriptor` and `test/assets.elisa` in commit `f17ace0`; `../Elisa-compiler/scripts/elisac_stage1.sh -emit exe -o build/assets-test test/assets.elisa && build/assets-test` passed, including path-rename, content-change, schema, variant, and malformed-identity cases.
- [x] **A02 · P1 · Incremental asset database** — After: A01.
  Extend the existing SQLite catalogue into a transactional dependency graph, import diagnostics, and deterministic cook cache; select the content-hash implementation from the chosen stack. Done: interrupted cooks recover, concurrent requests deduplicate, and reproducible cache hits survive process restart.
  Evidence: [`docs/validation/asset-catalogue.md`](docs/validation/asset-catalogue.md), `scripts/cook_assets.py --self-test` passed (`7 crafted + 96 fuzzed documents rejected` plus rollback/concurrency/cache checks), real `python3 scripts/cook_assets.py "$PWD"` produced schema-2 catalogue rows, and `record_validation_assets.py` accepted the dependency/cache invariants; committed with the implementation.
- [x] **A03 · P1 · Runtime package and virtual filesystem** — After: A01, F04.
  Evolve current cooked packages into bounded indexed bundles with zstd, alignment, versioning, overrides, and async-friendly reads. Done: runtime loading works outside the checkout; traversal, mount-escaping symlinks, overlapping sections, decompression bombs, and missing dependencies are rejected before allocation or upload. Progress: `native/virtual_package.h` parses bounded version-1 `ELPK` indexes, decompresses bounded zstd sections, verifies each section's CRC-32 after decoding, and resolves safe logical names through explicit override roots before the shipped base root with generation metadata. `native/package_manifest.h` parses bounded dependency manifests and computes deterministic prerequisite-first order while rejecting cycles and missing nodes; `native/virtual_file_service.h` validates that order in its worker pump and adds bounded coalesced requests, cancellation, pump budgets, stale mount-generation rejection, dependency-generation tokens, and asynchronous pumps. The native package gate covers worker scheduling, transitive ordering, cycles, corrupted-payload rejection, and mount-escaping symlink rejection where symlinks are available. Python cookers now emit deterministic ELPK bundles with sorted manifests, compressed mesh sections, and any generated texture variants; the native geometry loader selects a CRC-checked `mesh` section by ELPK magic while retaining loose `.pkg` compatibility. The package-format test and SDL3 snapshot smoke exercise the writer and reader. `scripts/packaged_maze_smoke.py` stages only the native maze executable and its ELPK bundle outside the checkout. It runs them under a `sandbox-exec` profile that denies all checkout reads and writes. The maze self-test passes, and a control run shows the checkout's own bundle is unreadable. A missing bundle, a symlink escaping the project root, and a corrupted `mesh` section all fail asset registration. `scripts/render_scene_native_smoke.py` runs these cases after the maze smoke. Evidence: [`docs/validation/packaged-loading.md`](docs/validation/packaged-loading.md). Shaders and dylibs still come from the Wicked checkout and Homebrew (R13, Q02). Cookers package PNG, JPEG, and bounded 2D Basis KTX2 images as ELPK sections in glTF mesh bundles or image bundles. `RenderScene::register_snapshot_bundle_texture_asset` resolves the bundle under the project root, CRC-checks the section, and bounds the image header. PNG/JPEG decode to CPU pixels; KTX2 stays encoded until owner-thread material upload. Async requests decode PNG/JPEG on the worker and retain KTX2 bytes after bounded reads. Source-section bytes and decoded pixels each have a 256 MiB cap. Material registration creates the mipmapped Wicked texture and schedules block compression on the owner thread. The native maze's wall material now uses a brick texture from its bundle. Missing, non-2D KTX2, oversized-header, corrupted, escaping and undecodable sections fail, and three remaining validation mutations cover dimension bounds, ID/path conflicts, and byte-accounting cleanup. Evidence: [`docs/validation/bundle-textures.md`](docs/validation/bundle-textures.md). Asset cooks declare `dependencies` on other bundles in the project. The runner validates the declarations, cooks dependencies first and writes each name into the bundle's manifest relative to the declaring bundle; a new `images` importer cooks texture-only bundles. `RenderScene::create_mesh` and the snapshot mesh and texture loaders walk each bundle's dependency closure under the project root before reading a section. A missing, cyclic, escaping or corrupted dependency, or a closure over 16 bundles, fails with `AssetLoadFailure`. The closure bound now counts bundles still on the search path, so a long chain is bounded before recursion reaches its end. The maze wall texture moved into its own bundle, which the tile bundle declares; the packaged maze fails registration when that bundle or a declared dependency is missing. The package probe rejects a zstd bomb both ways: an index admitting an 80 MiB section fails the 64 MiB bound, and one declaring 4 KiB fails decoding into its 4 KiB buffer. Evidence: [`docs/validation/bundle-dependencies.md`](docs/validation/bundle-dependencies.md). Shaders and dylibs outside the checkout remain R13 and Q02.
- [x] **A04 · P1 · Asynchronous resource loader** — After: A02, A03, F06.
  Implement request/coalesce/cancel/prioritize, worker decode, device-thread upload, residency, and budgeted eviction. Done: unloading during read/decode/upload and failed dependencies leave no leaked handle; frame threads do not block on file IO and placeholder behavior is explicit.
  Progress: `src/assets/loader.elisa` and `test/asset_loader.elisa` define bounded generation-checked request coalescing, priority updates, explicit queued/decoding/uploading/resident stages, cancellation, placeholders, and byte-budget eviction. `native/native_resource_loader.h` connects these stages to `VirtualFileService` and the Wicked device phase: the native gate proves coalesced compressed reads, bounded decode, real texture upload, stale dependency rejection, resident release, generation-safe reuse after cancellation/failure, upload telemetry, and worker-future IO separated from caller-thread upload. `VirtualFileService` marks requests as `Reading` under a short lock, performs path resolution, manifest traversal, package IO, decompression, and checksum work outside the lock, then rechecks mount epoch and dependency generation before publishing. A fixed two-thread worker pool drains a bounded pump queue; service destruction joins the workers before its state is released. Queued reads cancel immediately; in-progress ELPK section reads use shared tokens and stop at 64 KiB boundaries. The 32 MiB package probe observes the first chunk, cancels before EOF, and verifies no result is published; the CRC-32 verifier uses a table lookup per byte. The production render path also loads asynchronously: `RenderScene::request_snapshot_mesh_asset` and `request_snapshot_bundle_texture_asset` return without file IO. A bounded one-thread worker (`native/snapshot_asset_worker.h`) resolves the path, checks the dependency closure, reads mesh and texture sections, and decodes PNG/JPEG images away from the owner thread. Both mesh and texture readers now receive worker checkpoints and stop at the next 64 KiB read or zstd decode boundary. The native probe cancels a worker during a 32 MiB bundle-texture section read and verifies that no encoded texture is returned. `pump_snapshot_assets` adopts finished loads on the owner thread under the synchronous slot and byte budgets. Rows and materials that name a loading asset fail with `AssetPending`, and no placeholder is substituted. Unregistering cancels a queued, parked, running or finished load, and shutdown releases and joins a parked job. The maze client requests its tile and texture bundles this way, assigns priority 100 to the tile mesh and 50 to the wall texture, and keeps presenting frames under a loading overlay; the packaged maze's nine cases pass through the worker. Native tests reach each cancellation point with worker holds, leave no request, job or byte behind, catch seven render-scene mutations, and force mesh and bundle-texture budget pressure. Resident geometry and texture entries carry deterministic LRU stamps; installation preflights a complete reclaim, evicts only entries unreferenced by live scene rows or materials, and touches assets when requests or committed rows use them. A ThreadSanitizer harness in the native gate runs the worker probe, which catches seven worker mutations and verifies descending queue priority, FIFO ties and queued reprioritization. Evidence: [`docs/validation/async-snapshot-assets.md`](docs/validation/async-snapshot-assets.md). Progress on 2026-09-22: ELPK zstd sections now stream through bounded 64 KiB input/output chunks, with a 64 MiB frame-window ceiling; worker cancellation is checked during decompression and partial output is discarded. The native package gate covers a 4 MiB decoded section, cancellation, checksum validation, truncated frames, and decompression-bomb rejection. Remaining: PNG/JPEG decode is not interruptible, and synchronous compatibility APIs still read and decode inline.
- [ ] **A05 · P1 · Complete glTF scene import** — After: A02, F07.
  Extend cgltf import to scene hierarchies, mesh primitives, indices, materials, cameras, lights where supported, skins, morphs, and animation through normalized engine assets. Done: a representative authored scene renders correctly; unsupported extensions fail visibly rather than silently losing content.
  Progress: `src/assets/scene.elisa` defines a bounded normalized scene contract for stable nodes, primitives, cameras, lights, skins, morph metadata, parent-cycle checks, and unsupported-extension rejection; `test/asset_scene.elisa` covers the contract. `native/asset_import.h` now traverses cgltf nodes/primitives/cameras/lights/skins/animations/morph targets, validates PBR factors/alpha metadata and texture references, recognizes supported Basis texture extensions, rejects unsupported or malformed content, and decodes a bounded first primitive into finite engine arrays. The Wicked gate uploads that authored primitive through a real mesh component and renders it as the goal marker while proving a pinned material fixture.
  Progress: multi-primitive meshes now draw each primitive with its own material. `scripts/cook_gltf_geometry.py` cooks up to 16 triangle primitives of one static mesh node into index subsets bound to up to 16 material slots, sharing vertex blocks that primitives share and generating missing normals. `native/cooked_geometry_package.h` requires the subsets to partition the index stream and name existing slots for both static and skinned geometry. `RenderScene::register_snapshot_material_set` registers one material per slot under an ID in the material ID space; a snapshot row names a material for every slot or a set whose size matches the mesh, and each Wicked subset draws with its slot's material. Listed materials and sets in use can't be unregistered. The render smoke renders a three-strip panel and a two-subset skinned strip, reads each subset's material binding, reads the panel's colors from the frame, swaps sets, and checks shadow policy, rejections and cleanup. A sanitized loader test runs 75 cases. All eleven render mutations and the expanded loader mutations are caught. The test also found that no skinned snapshot row could commit since shared static meshes landed, which is fixed. The smoke's frame probes now wait for Wicked's background pipeline compiles, which had left every mesh undrawn on a loaded machine. Evidence: [`docs/validation/gltf-material-subsets.md`](docs/validation/gltf-material-subsets.md). Remaining: runtime glTF cooking of multi-material skinned meshes, cameras, lights and animation.
  Progress: cooked meshes now carry their glTF material factors. `scripts/cook_gltf_geometry.py` packs each declared material's base color, metallic, roughness, emissive, alpha cutoff, opaque or blended alpha mode, and sidedness into a 48-byte slot record, using glTF defaults for absent factors. Textures, `MASK`, extensions, `extras`, unknown properties and out-of-range or malformed factors fail visibly. `native/cooked_geometry_package.h` requires the records to come with subset records, to match the slot count, and to hold in-range factors, a known mode and one flag bit. `RenderScene::snapshot_mesh_material_count`, `register_snapshot_mesh_material` and `register_snapshot_mesh_materials` register a resident mesh's cooked materials as ordinary materials, or all of them plus a set, under game-chosen IDs. They follow the material no-op and conflict rules, and a failed whole-mesh registration removes every material it created. Material and set registration now share one locked implementation. The render smoke registers the panel and a blended glass variant from their cooked factors, checks each Wicked material and the drawn strip colors, and covers refusals, rollbacks and in-use protection. The loader test runs 75 cases, including valid skinned slot records. All 15 new render mutations, the expanded loader mutations and 13 cooker mutations are caught. Evidence: [`docs/validation/cooked-slot-materials.md`](docs/validation/cooked-slot-materials.md). Remaining: cooked material textures and alpha masks, node hierarchies and transforms, multiple meshes, multi-material skinned meshes, cameras, lights and animation in the runtime cooker.
  Progress: the runtime cooker now bakes a static glTF scene's node hierarchy into one cooked mesh. The new `scripts/cook_gltf_nodes.py` validates up to 256 nodes with matrix or TRS transforms, one scene, single parents, reachability and a placement for every mesh, then flattens the tree depth first. `scripts/cook_gltf_geometry.py` transforms each placement's positions, carries normals by the cofactor matrix, swaps winding under mirroring transforms, copies identity placements byte for byte, merges adjacent same-slot subsets up to 16, and bounds vertex and index totals across placements. The package format, loader and runtime are unchanged. A fixture places a red strip twice under a translated group, turns a downward-facing green strip up by rotation, and mirrors a blue strip by negative scale. The cooker self-test checks the exact baked strips, TRS and matrix conventions against Rodrigues' formula, byte-identical equivalent authorings, and 45 rejections. The loader test runs 46 cases. Render smoke group 228 draws the baked panel through single-sided materials, reads each strip's color from the frame, and checks that a half turn swaps them. All 42 cooker mutations fail the self-test, and six of the eight that change the frame fail group 228. The smoke also showed that the render service drew Elisa's right-handed world mirrored, which predated this slice. The service now reflects into Wicked's frame at the boundary: [`docs/validation/render-scene-handedness.md`](docs/validation/render-scene-handedness.md). Evidence: [`docs/validation/gltf-node-hierarchies.md`](docs/validation/gltf-node-hierarchies.md). Remaining: cooked material textures and alpha masks, multi-material skinned meshes, cameras, lights and animation in the runtime cooker.
  Progress: cooked meshes now carry their glTF material textures and alpha masks. `scripts/cook_gltf_textures.py` resolves each material's base-color, normal, metallic-roughness, emissive and occlusion textures into five runtime slots. Occlusion may share the metallic-roughness image or remain separate in Wicked's `OCCLUSIONMAP`; the slot flags it. Sampling must be trilinear and repeating through `TEXCOORD_0` with unit scale and strength. Images must be embedded PNG/JPEG or Basis KTX2 selected through `KHR_texture_basisu`; all cooked texture and sampler declarations must be used. `MASK` cooks when the slot has a base-color image. `cook_gltf_asset.py` writes each sampled image as a section of the mesh's `.elpk`, and the package lists the sections and five image references per slot. `native/cooked_slot_materials.h` validates names, references, coverage and needed images, requires a bundle holding every section, and records each section's checksum. `RenderScene::snapshot_mesh_texture_count` and `register_snapshot_mesh_texture` register a mesh's images from its own bundle, and refuse a bundle rewritten since the mesh loaded. Cooked material registration binds each slot to the one texture ID holding its image, enables Wicked occlusion, and refuses missing, foreign or ambiguous images before creating anything. Render smoke group 230 draws the masked cutout over an emissive backdrop, a lit, normal-mapped and occluded painted strip, and an emissive glow strip, and reads each half-strip's color from the frame. The loader test runs 87 cases. Every render mutation except one equivalent UV-set mutation is caught, including a texture offset that only the frame detects. So are 18 of 20 loader mutations, with 2 equivalent, and all 36 cooker mutations. Evidence: [`docs/validation/cooked-material-textures.md`](docs/validation/cooked-material-textures.md). Remaining: multi-material skinned meshes, cameras, lights and animation in the runtime cooker.
  Progress: the runtime glTF cooker now emits v3 packages for bounded multi-material skinned meshes. `scripts/cook_gltf_skin.py` validates one skin, up to 64 distinct joints, parent ordering, TRS rest transforms, float32 four-weight influences, and optional float32 inverse-bind accessors; `scripts/gltf_skin_self_test.py` generates the deterministic three-subset/two-material panel fixture and rejects cameras, posed mesh nodes, malformed influences, unsupported skin semantics, and unsupported animation channels. `scripts/test_geometry_subsets.py` feeds that generated package to the sanitized production C++ loader, and the native subset smoke accepts its v3 rig, cluster map, rest transforms, and material subsets. Mesh-node transforms remain identity because the existing runtime uploader owns the entity pose. Evidence: [`docs/validation/gltf-material-subsets.md`](docs/validation/gltf-material-subsets.md). Remaining: cameras, lights, morph targets, and complete multi-mesh scene output in the runtime cooker.
  Progress: glTF animation channels feed the v3 runtime clip format shared with FBX. The cooker validates up to twenty-four bounded named clips, translation/rotation/scale channels on skinned rig/helper nodes, morph-weight channels on placed morph meshes, required finite float32 time bounds, and LINEAR, STEP, or CUBICSPLINE interpolation before sampling fixed 30 Hz clips. LINEAR quaternion tracks use spherical interpolation; cubic quaternion results are normalized. Morph outputs accept float32 and normalized 8/16-bit accessors, and each placement keeps its node/mesh default unless its own track animates it. Unskinned morph-only clips are supported. The sanitized loader asserts clip metadata and stream bounds; cooker regressions cover cubic TRS/weights, quantized weights, placement defaults, and duplicate/matrix-target rejection. Dense morph deltas, bounded camera and `KHR_lights_punctual` records, imported-scene handles, and their rollback/cleanup checks remain as described below.
  Progress on 2026-09-22: the bounded cooker path is covered by deterministic geometry, hierarchy, skin, morph, texture, camera/light, and animation self-tests plus the SDL3/Metal imported-scene smoke. `RenderScene::create_mesh` now uploads every placement's vertex, index, subset, tangent, skin, and morph ranges as its own Wicked mesh. Skinned placements share their root instance's armature; pose submission applies morph weights to every placement and rejects mismatched placement streams before changing pose state. The generated two-placement skinned/morphed fixture verifies the sliced ranges and shared deformation state. At this point snapshot rows still used the flattened compatibility path. Evidence: [`docs/validation/skinned-mesh-placements.md`](docs/validation/skinned-mesh-placements.md). Remaining: multiple skins per scene, non-identity skinned node transforms, and per-placement snapshot upload for skinned/morphed geometry.
  Progress: cooked packages now retain bounded multi-mesh placement metadata alongside the flattened render payload. `mesh_count` and fixed-stride records preserve each source mesh index, node index, and world transform in depth-first order; the native loader validates record bounds, finite transforms, complete mesh coverage, and malformed metadata. The hierarchy self-test and sanitized loader cover deterministic three-mesh/four-placement output and stride/count rejection. `RenderScene::snapshot_mesh_placement_count` and `snapshot_mesh_placement` expose those records to Elisa clients, with the ABI using scalar component reads because stage1 cannot lower one extern with a dozen mutable float outputs. The public `RenderScene::imported_scene` API reports live child counts; opaque root-checked mesh and camera handles now select and edit authored child resources while keeping Wicked entities private. Evidence: [`docs/validation/gltf-node-hierarchies.md`](docs/validation/gltf-node-hierarchies.md).
  Progress: cooked placement records carry contiguous vertex/index ranges and overlapping subset ranges in an 80-byte stride. `RenderScene::create_mesh` uploads each range as an attached Wicked mesh entity and removes every child on destroy or rollback; multi-placement clones remain refused until clone semantics cover the full set. The SDL3/Metal smoke creates the four-placement hierarchy, checks four live mesh entities under one public handle, and verifies cleanup. `RenderScene::imported_scene` provides bounded public child counts, imported camera activation, mesh placement edits, and authored-light shadow control. Evidence: [`docs/validation/gltf-node-hierarchies.md`](docs/validation/gltf-node-hierarchies.md).
   Progress on 2026-09-23: glTF `inverseBindMatrices` are now retained in skin-palette order from the accessor through cooked v3 packages and into Wicked's `ArmatureComponent`. The cooker uses the first `joint_count` entries when the accessor has surplus entries and emits an explicit identity matrix per joint when the optional accessor is absent, matching the glTF default. The cooker and bounded native loader reject malformed, non-finite, projective, singular, incomplete, and mis-sized streams; inverse binds are converted into Wicked row space with the shared X-axis reflection, and older/FBX packages keep their derived-bind fallback. The native animation submission bridge now applies absolute pose matrices after the same coordinate conversion instead of multiplying each update onto the prior rest/pose transform. Deterministic cooker assertions cover authored, omitted, and surplus matrix accessors; 95 sanitized loader cases, the live palette probe, the SDL3/Metal imported-scene smoke, and the packaged maze smoke passed. Evidence: [`docs/validation/gltf-inverse-bind-matrices.md`](docs/validation/gltf-inverse-bind-matrices.md). Remaining: multiple skins per scene, non-identity skinned mesh-node transforms, and per-placement snapshot upload for skinned/morphed geometry.
  Progress later on 2026-09-23: static snapshot rows now create one shared Wicked mesh per cooked placement, slice and rebase local indices, clip material subsets to the placement's index range, and attach child draw objects beneath a single public root. Retirement and injected commit failure remove the children and release shared-mesh references. The SDL3/Metal native smoke checks placement-local index/subset/material data, the four-placement render, and rollback. Skinned and morphed snapshot rows retain the flattened path. Evidence: [`docs/validation/snapshot-mesh-placements.md`](docs/validation/snapshot-mesh-placements.md). Remaining: multiple skins per scene, non-identity skinned mesh-node transforms, and skinned/morphed per-placement snapshot upload.
  Progress later on 2026-09-23: the glTF cooker ignores skinned mesh-node transforms in cooked vertex, normal, tangent, index, and morph streams while preserving each authored placement transform as metadata. It accepts finite singular mesh-node transforms too, which glTF also says to ignore. The two-placement skin fixture gives its second placement a translation, rotation, and nonuniform scale; cooker tests verify these values do not alter the streams, and the SDL3/Metal animation smoke verifies both placement meshes receive the same vertices. Transformed non-joint ancestors of joint nodes remain rejected because their effect on global joint poses is not represented in the runtime rig. Evidence: [`docs/validation/gltf-inverse-bind-matrices.md`](docs/validation/gltf-inverse-bind-matrices.md). Remaining: multiple skins per scene, transformed non-joint ancestors of joint nodes, and skinned/morphed per-placement snapshot upload.
  Progress later on 2026-09-23: snapshot-created skinned and morphed meshes now upload every cooked placement as an independent Wicked mesh. The root placement owns the armature; child placements retain sliced vertex, index, subset, skin-influence, and morph streams and share that armature. Animation submission now includes snapshot children and applies the same pose and morph weights across the group. Native smoke coverage injects a failed transaction after creating the animated root and child, confirms rollback, retries, submits a pose through the C ABI, verifies the child deformation state, and clears all resources. The SDL3/Metal RenderScene native gate passed; the separate maze application and packaged-maze stages were skipped due to a Metal teardown hang in the preceding run. Evidence: [`docs/validation/snapshot-mesh-placements.md`](docs/validation/snapshot-mesh-placements.md).
  Progress later on 2026-09-23: the glTF rig cooker keeps transformed or animated non-joint ancestors as parent-ordered helper transforms, including skeletons on sibling branches under a transformed scene parent. A synthetic mesh-relative basis preserves branch offsets without moving cooked meshes; shared ancestors still cancel. The cluster map refers only to actual palette joints. TRS-equivalent affine matrix nodes are decomposed, while shear-producing bases are rejected. Native limits are 64 total palette bones and 256 rig nodes. The cooker combines distinct skin rigs per mesh placement, offsets vertex palette indices, duplicates animation tracks across matching rig branches, and supports static placements beside skinned placements using a synthetic identity bind bone. Cooker assertions cover sibling and separate roots, shared-transform cancellation, nested helpers, animated rest nodes, matrix conversion, hierarchy order, shear rejection, multi-skin remapping, and static bind weights. The sanitized C++ loader accepts the multi-skin and mixed-placement packages plus a 65-node rig with two palette bones (100 cases, 0 failures). The SDL3/Metal smoke compiled and passed sibling-branch animation submission, separate-root mesh upload, multi-skin palette remapping, mixed-placement armature and morph-only animation probes, then stopped at the later overlay-hide readback with test status 134, after those probes. The 2026-09-23 cooker checks now also cover cubic TRS and weight tracks, LINEAR quaternion SLERP, normalized integer morph outputs, per-placement defaults, and unskinned morph-only clips. Evidence: [`docs/validation/gltf-skin-hierarchies.md`](docs/validation/gltf-skin-hierarchies.md).
- [ ] **A06 · P1 · Production texture pipeline** — After: A03, F08.
  Extend KTX2/Basis handling to mip chains, color spaces, normal maps, alpha policy, cubemaps, and supported GPU-native transcodes. Done: runtime selects queried formats, avoids unnecessary RGBA expansion, and validates malformed containers and texture memory budgets.
  Progress: `native/texture_format_policy.h` selects formats from queried device support; `native/ktx2_upload.h` transcodes opaque color to BC1, alpha color to BC7 or BC3, and normal data to BC5_RG when available, with RGBA8 as a channel-preserving fallback. Color uploads preserve linear/sRGB metadata. Loose and ELPK-section KTX2 assets registered through RenderScene use this loader and cache GPU resources by material role. Bundle workers retain bounded KTX2 bytes and owner-thread material upload transcodes them. Bounded mip chains and six-face cubemaps upload in Wicked's slice-major order, with a 64 MiB container and decoded-payload limit plus per-mip checks. Basis fixtures verify BC1/BC5/BC7/BC3 targets, authored normal X/Y preservation, sRGB metadata, alpha values, distinct cube-face colors, and rejection of 15 malformed header/DFD/key-value/mip-index mutations; focused Wicked and Elisa material smokes verify device-selected formats, bundle and loose-asset registration, normal-data preservation, alpha safety, cube shape, and rejection of empty or oversized files before allocation. Remaining: broader HDR/compressed formats and deeper malformed KTX2 structure coverage.
  Progress on 2026-09-23: `scripts/ktx2_container.py` now checks the DFD/KVD/SGD ranges and layout, KVD entries and padding, level bounds/non-overlap, and scheme-specific uncompressed lengths before KTX2 images enter an ELPK bundle. Python and native upload policy both reject array textures the runtime cannot preserve. Seventeen malformed section-layout fixtures and valid metadata, BasisLZ, and multi-level layouts pass the cooker tests; the glTF cooker self-test, CPU Basis transcodes, geometry loader, module/dependency checks, and full ElisaScript gate passed. Validation: [`docs/validation/ktx2-structure.md`](docs/validation/ktx2-structure.md). Full GPU upload coverage remains unverified because the native render smoke aborts before texture upload; broader HDR/compressed-format support remains open.
  Progress: `src/backend/material.elisa` and `test/material.elisa` define validated PBR texture slots, scalar factors, alpha policy, and normal-map identity at the engine boundary; native KTX2 mip/color-space policy remains.
- [ ] **A07 · P1 · Mesh optimization and LOD cooking** — After: A05.
  Use meshoptimizer for vertex/index optimization, simplification, compression, and optional meshlet data only where the selected render path consumes it. Done: screen-error LOD selection preserves boundaries and material subsets; visual errors, bytes, cook time, and render cost are measured.
  Progress: `src/assets/lod.elisa` defines a bounded deterministic LOD chain with screen-error selection, material-subset preservation, duplicate/capacity checks, and invalid-order rejection; `test/asset_lod.elisa` is part of the shared gate. `native/meshopt_probe.h` validates simplification on an authored primitive, and pinned meshoptimizer cooks high-density FBX geometry with a configured triangle ceiling and compacted vertex streams. The FBX cooker keeps vertex-cache reorder only when its 16-entry ACMR improves (the generated 512-triangle grid improves from 1.0625 to 0.6543) and remaps positions, normals, UVs, tangents, and optional skin streams together after tangent generation. The supplied 97,679-vertex walking mesh improves modeled fetch bytes from 9,089,280 to 7,815,936; repeated cooks remain byte-identical and package validation checks normalized skin rows. The 3,077,694-triangle Arc Gate cooked to 12,000 triangles, 10,009 vertices, and an 833,098-byte tangent package. Static GLB import uses bounded Blender-side pre-reduction and production FBX cooking; its fixture reduces 3,042 triangles to 55 with 37 vertices under a 64-triangle limit. The early single-variant GLB step did not yet establish visual quality or runtime selection. See [`docs/validation/fbx-import.md`](docs/validation/fbx-import.md), [`docs/validation/glb-character-import.md`](docs/validation/glb-character-import.md), and [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md).
  Progress on 2026-09-23: the glTF scene cooker now invokes pinned meshoptimizer to reorder triangles within each placement/material intersection, preserving the triangle set, winding, subset boundaries, and all vertex-side streams. The shuffled two-placement fixture's 16-entry ACMR improves from 2.8564 to 0.6602; repeated cooks match, and the production package loader passes 101 cases. Evidence: [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md). SDL3/Metal smoke exited 134 after Wicked initialization, before reporting render-group results.
  Progress later on 2026-09-23: the first glTF simplification increment added one reduced static variant, per-placement/material boundaries, locked borders, and rejection for skin, animation, and morph input. Subsequent increments compacted/remapped vertices and added deterministic multi-level package chains; earlier single-variant limitations are historical. Commands and limits: [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md).
  Progress later on 2026-09-23: the portable chain now uses full-width `Assets::AssetId` values for meshes and material subsets, rejects invalid/non-live levels and non-finite thresholds, and validates selection budgets. Tests cover IDs sharing the same low word and exact lookup by the high word, along with NaN and empty-chain rejection. The focused Elisa test, module-hygiene, source-length, and dependency-manifest checks passed. The first full-gate attempt also caught an immutable `world` binding at a mutating compaction call; `test/world.elisa` now marks that binding mutable. The subsequent shared script passed end to end, including proof replay and release packaging, through the installed stage1 wrapper and Homebrew Python. Exact command: [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md). Multi-LOD package emission, runtime selection, visual review, and performance comparisons remain.
  Progress later on 2026-09-23: static glTF LOD cooking now compacts vertices per placement and remaps positions, normals, UVs, tangents, and indices together while updating placement ranges. It rejects placements with overlapping source vertex ranges. The deterministic test reduces 1,152 triangles to 576 and 625 vertices to 337, shrinking those four attribute streams from 30,000 to 16,176 bytes. The 101-case sanitized production loader suite accepts the compacted multi-placement package and verifies index bounds; the GLB static cook regression also passes. Evidence and commands: [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md). It remains a single-variant cook; multi-level packaging, screen-error metadata/runtime selection, visual quality review, and render-cost measurements remain.
  Progress later on 2026-09-23: `--lod-ratios` now emits a full-detail package plus descending simplified variants and a deterministic sibling manifest. Packages are content-addressed; the manifest records source/package hashes, actual byte sizes, geometry counts, attribute bytes, and measured relative geometric error with a cumulative monotonic budget. The production reader accepts all three generated levels in the sanitized 104-case suite, and cooker tests verify repeated outputs and manifest hashes. Evidence: [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md). Runtime manifest loading, conversion from normalized geometry error to projected screen pixels, renderer selection, visual quality review, and render-cost measurements remain.
  Progress later on 2026-09-23: the cooker manifest records full-detail extent; `AssetLod::projected_error_pixels` and `native/lod_selection.h` project cumulative geometric error into pixels and select a coarser level with 10% switching hysteresis. The native chain loader verifies exact SHA-256/size, root confinement, and stable static layout. Snapshot sync/async registration retains validated chains, and the RenderPath now reevaluates each static row from the active camera and world scale, applying `2^lod_bias` to its one-pixel target and switching every placement through level-specific shared meshes. Unit coverage checks thresholds and hysteresis; chain tests reject malformed manifests, package tampering, and root escapes. SDL3/Metal smoke builds currently exit 134 immediately after Wicked initializes and logs creation of its first 256 MiB Metal buffer, before the new live selection assertions report. Visual quality and render-cost measurements remain unverified. Evidence: [`docs/validation/mesh-lod.md`](docs/validation/mesh-lod.md).
- [ ] **A08 · P1 · Tangents and authored lightmap UVs** — After: A05.
  Integrate MikkTSpace and xatlas as offline stages with deterministic settings, seam handling, and metadata. Done: mirrored UV normal mapping and a UV-overlap fixture validate output; tools are not pulled into the game runtime unnecessarily.
  Progress: the FBX cooker generates MikkTSpace float4 tangent frames after simplification, splitting tangent discontinuities and preserving skin weights when vertices split; the bounded cooked-package reader validates the optional stream and Wicked mesh upload consumes it. The 512-to-128-triangle planar-UV self-test checks normalized, orthogonal +X tangents and byte-identical output. glTF tangent seams and authored lightmap UV generation remain. Evidence: [`docs/validation/fbx-import.md`](docs/validation/fbx-import.md) and [`docs/validation/render-scene-textures.md`](docs/validation/render-scene-textures.md).
  Progress on 2026-09-22: the glTF cooker now emits the same bounded float4 tangent stream for textured and untextured meshes, derives frames from final transformed positions/UVs when `TANGENT` is absent, and preserves authored tangent handedness through node transforms. Malformed tangent accessors are rejected, the production loader validates the stream, and the SDL3/Metal smoke loads the tangent-bearing textured bundle. MikkTSpace seam equivalence, mirrored-UV reference captures, xatlas lightmap UV generation, and lightmap metadata remain.
  Progress on 2026-09-22: tangent-frame generation can now recover a missing or zero imported vertex normal from adjacent triangle geometry before orthogonalizing its tangent. This keeps rejection for malformed indices and unusable geometry while allowing static props with incomplete normals to cook.
  Progress on 2026-09-23: pinned MikkTSpace and xatlas source files to immutable upstream commits and added them to the native dependency manifest. MikkTSpace is integrated into the FBX cooker and remains outside the runtime link; glTF integration and xatlas generation remain.
  Progress later on 2026-09-23: the sanitized mirrored-UV test confirms opposite handedness across a shared edge, zero-normal recovery, and deterministic 4-to-6-vertex splitting. The FBX self-test runs it before cooking its simplification fixtures. glTF MikkTSpace integration, xatlas lightmap UV generation, and lightmap package metadata remain.
  Progress on 2026-09-23: MikkTSpace now filters geometrically or UV-degenerate triangles and supplies stable orthogonal tangent frames for their corners. Missing normals recover from adjacent faces or use a finite fallback for unused/fully degenerate points. Native tests cover degenerate charts and triangles plus unreferenced control points; the dedicated MikkTSpace and FBX cooker self-tests pass.
- [ ] **A09 · P2 · FBX import with ufbx** — After: A05, C01.
  Implement FBX-to-normalized mesh/skeleton/animation/material conversion with documented axis/unit and unsupported-feature policy. Done: the same animated character imported from glTF and FBX has comparable bind pose, playback, and material assignment; malformed input is bounded.
  Progress: ufbx v0.23.0 is pinned by commit and SHA-256 in the dependency manifest. `native/fbx_asset_import.h` performs strict, memory-bounded FBX parsing, normalizes axis and units to right-handed +Y-up metres, and decodes the largest triangle mesh into finite indexed positions/normals/UVs. Static vertices use position/normal/UV indexing; skinned vertices add separate four-influence streams, preserving seam and weight distinctions. The importer preserves a parent-ordered skeleton (up to 64 joints), cluster palette order, normalized weights, and bounded 30 Hz samples for non-empty clips. The cooked-package reader validates these streams; static assets use v2 and skinned assets use v3. `RenderScene::create_mesh` uploads skin weights, creates the Wicked armature and inverse-bind palette, and retains per-instance clips. Elisa exposes `play_animation`, `stop_animation` (blend to rest pose), and `advance_animation`; an active game run verified live pose updates. Import tests check that walking samples change joint poses. On 2026-09-20 the supplied walk/run assets (34-bone rigs, 83,522 triangles, two source clips each) and the 3,077,694-triangle fence passed bounded import tests; the cooked walking package retains 34 clusters, 36 hierarchy joints, one non-empty clip, and 97,679 vertices. Dense offline imports have 1.5 GiB temporary, 3 GiB parsed-scene, and 1 GiB extracted-geometry ceilings. The cooker writes hash-carrying packages with deterministic tangent frames and optionally simplifies unskinned meshes via pinned meshoptimizer; the project runner forwards a validated triangle limit. `scripts/cook_glb_asset_blender.py` now retargets imported GLB clips through per-joint full rest-orientation alignment, scales root travel by rig height, and writes target-local quaternion channels without constraint side effects. Complete multi-mesh scene cooking, FBX material discovery/subsets, cross-fade between two authored clips, asynchronous asset residency, and production ozz integration remain.
- [ ] **A10 · P1 · Safe asset hot reload** — After: A04, W04.
  Stage replacement dependency graphs and swap generations at a safe frame boundary, retaining old GPU/audio resources until consumers finish. Done: edited textures/materials/meshes update live; a broken replacement preserves the last good scene and reports import errors.
- [ ] **A11 · P2 · Collision and navigation cook artifacts** — After: A05, P02, N01.
  Cache Jolt collision data and Recast tiles by geometry/settings/version; add CoACD convex decomposition for selected dynamic concave assets. Done: runtime never repeats offline decomposition; hull error/cost limits and incompatible artifact rebuilds are tested.
- [ ] **A12 · P2 · Asset import fuzzing and workers** — After: A05, A06.
  Expand bounded/fuzzed corpus coverage across model, image, animation, and package import; isolate expensive untrusted imports in cancellable worker processes. Done: corrupt inputs, timeouts, and crashes produce reproducible diagnostics without corrupting the catalogue or editor session.

## R — Wicked rendering as an engine service

- [x] **R01 · P0 · Render extraction and entity mapping** — After: F04, F06, F07.
  Replace maze/manifest-specific rendering with an Elisa-owned frame snapshot or batched change stream and explicit one-to-many render IDs. Done: spawn/update/despawn reaches a persistent scene; vendor-only entities never become gameplay identities, and render updates cannot advance gameplay independently.
  Progress: commit `f2266fd` adds `src/runtime/world_rendering.elisa`, which extracts bindings from live `World` entities, validates references, supports one-to-many render IDs, and prunes despawned or replaced-world rows. Portable tests cover identity, updates, invalid bindings, and capacity. `test/render_snapshot.elisa` and `native/render_snapshot_bridge.h` are gated; the probe bridge validates transactional row batches and rolls back newly created entities on staging failure. `src/runtime/render_scene.elisa` exposes a persistent Wicked scene service for primitive creation, transforms, color/emission/visibility, camera control, resize, bloom control, and generation-checked handles. The live snapshot presenter submits a bounded native transaction carrying gameplay epoch/entity ID, stable render ID, mesh/material IDs, and transform. It validates retained and retired handles, creates replacements before applying updates or removals, and returns opaque handles only after commit. Asset identity changes recreate the native instance. Portable and SDL3/Metal tests inject failure after one staged creation, confirm the previous frame remains live, retry successfully, verify identity metadata reaches the native instance, and check despawn retirement. Gameplay transforms live in `World` registry rows; render bindings retain only per-render local attachment transforms, and extraction composes them. Tests cover transform validation, shared entity transforms across one-to-many render IDs, local offsets, update, despawn, and replacement. Evidence: [`docs/validation/elisa-render-scene.md`](docs/validation/elisa-render-scene.md). The maze client uses this snapshot path and its gameplay rows now resolve registered authored cooked meshes and PBR scalar materials. The center-pixel readback remains test-only in `native/render_scene_pixel_probe.h`; ordinary application links omit that symbol.
  Validation on 2026-09-20: `DEVELOPER_DIR=/Library/Developer/CommandLineTools ELISA_COMPILER_BIN="../Elisa-compiler/scripts/elisac_stage1.sh" python3 scripts/render_scene_native_smoke.py` passed on SDL3/Metal, including injected partial-creation rollback with native object-count and prior-identity checks, successful retry, asset/entity identity checks, despawn retirement, cooked mesh rendering, and path rejection. The ordinary application native smoke passed both lifecycle binaries. The full `elisascript scripts/check.elisascript` suite passed both Elisa Proof suites (17/17 and 6/6); source-length, module-hygiene, dependency-manifest, and `git diff --check` policies passed.
  Progress on 2026-09-21: `examples/maze/presentation.elisa` privately composes a `World`, render bindings, and key/door references from the public maze topology and gameplay markers. Its packaged headless session creates and extracts 40 render rows, synchronizes movement, key pickup, door unlock, and restart, and passes through `scripts/maze_game.elisascript`.
  Native integration follow-up on 2026-09-21: `test/maze_rendering_native.elisa` drives the maze win trace through the same `SnapshotPresenter` used by ordinary Elisa render clients. The SDL3/Metal smoke asserts 40 native instances at start, 39 after key pickup with the unlocked door material, 40 after restart, and zero after transactional clear.
  Interactive integration on 2026-09-21: `examples/maze/native_main.elisa` is now a regular project entry. It constructs the maze in Elisa `World`, updates snapshots on keyboard input, handles resize/suspend/close, and runs through `scripts/elisa_build_run.py`; it does not read `backends/scene_manifest.txt`. The separate `native_smoke_main.elisa` entry is wired into `scripts/render_scene_native_smoke.py` and passes a hidden SDL3/Metal win, restart, snapshot-count, transactional-clear, and shutdown run. Both ordinary project build and native smoke passed with the current macOS toolchain. Nested maze and trace references now use explicit module paths.
  Completed on 2026-09-21: `src/runtime/render_scene_assets.elisa` adds typed stable-ID registration for cooked mesh packages and PBR material descriptors. Native snapshot staging rejects unknown IDs; static rows share a Wicked mesh by mesh/material ID pair, bind the registered material entity, and preserve per-object transforms. Failed snapshot creation rolls back objects, joints, and unreferenced pair meshes. The maze project cooks `assets/maze_tile.gltf` into `assets/maze_tile.pkg`, registers seven authored materials, and maps its tiles to that shared mesh. The native smoke checks 24 vertices/36 indices, material factors, pair sharing, live-resource unregister rejection, rollback/retry, and final release. Skinned snapshot meshes remain per-instance. Snapshot texture IDs now require separate bounded registration and apply project-root-confined base-color, normal, packed-surface, or emissive resources to Wicked materials; nonidentical simultaneous metallic-roughness and occlusion maps are rejected until cooking can pack them. The glTF geometry cooker bakes a static node hierarchy of up to 256 nodes into one mesh, each placed mesh with up to 16 indexed triangle primitives bound to material slots (see A05) and POSITION/NORMAL/TEXCOORD_0 data; skins, morphs, material extensions, and extra attributes must fail visibly and remain scoped to A05/A06/R04; cooked material factors, embedded material textures and alpha masks are covered in A05. R01's extraction, authored-resource resolution, transactional update, and despawn criteria are met; the manual visible-controls check remains part of M0. See [`docs/validation/snapshot-authored-assets.md`](docs/validation/snapshot-authored-assets.md).
  Correction on 2026-09-21: `RenderSnapshot::snapshot_valid`, `WorldRendering::bindings_valid`, `World::Rollback::snapshot_valid`, and 13 other validators discarded their per-row accumulator loops and returned `true`. They now return the loop result. `snapshot_bind` rejects a render ID owned by another entity, and module hygiene rejects discarded accumulator loops. Regression tests fail on the old code. The full suite and the SDL3/Metal render smoke passed. See [`docs/validation/validator-loops.md`](docs/validation/validator-loops.md).
  Correction on 2026-09-21: `elisa_render_scene_v1_snapshot_stage` checked only the handle and the transform on a retained row (nonzero `existing_handle`). A changed mesh, material, material set or identity was therefore never drawn, and commit overwrote the instance's recorded IDs anyway. Staging now returns `INVALID_ARGUMENT` unless the row names the instance's gameplay epoch and ID, render ID, mesh ID, and material or material-set ID. To change any of them, retire the handle and stage a new row. `RenderSnapshotScene::sync` now compares gameplay identity too, so a render ID that passes to another entity or world epoch replaces the instance instead of retaining it. `test/render_scene_snapshot_retained_native.elisa` (render smoke group 195) covers retained rows, nine refused changes and the presenter path. Removing the native check fails it at case 112; removing the presenter's identity comparison fails it at case 213. See [`docs/validation/snapshot-authored-assets.md`](docs/validation/snapshot-authored-assets.md).
- [x] **R02 · P1 · Mesh/material/instance API** — After: R01, A04.
  Expose reusable static/skinned mesh instances, material slots, visibility, layers, and shared resources with batched updates. Done: hundreds of instances share one mesh/material; modifying or destroying an instance cannot corrupt another, and ABI traffic is measured.
  Progress: `src/backend/render_resources.elisa` provides generation-checked instance handles, shared mesh/material asset IDs, transform/visibility/layer updates, reference counting, and an atomic bounded instance-update batch; `test/render_resources.elisa` verifies independent updates, duplicate-batch rejection, and stale-handle rejection. `native/resource_handles.h` applies material color, visibility, and layer-mask updates through owner/generation-checked Wicked handles, with batch traffic telemetry and native coverage. Public `RenderScene::create_mesh` creates a checked Wicked instance from cooked positions, normals, tangents, UVs and indices. For skinned v3 packages it uploads four influences, creates the ordered joint hierarchy and inverse-bind palette, and retains sampled clips. Elisa calls `play_animation`, `stop_animation`, and `advance_animation` to select looping playback, blend to rest, and update each instance independently; `animation_progress` exposes the active clip's normalized time while returning zero for idle instances and `UnknownHandle` for retired ones. The live game, animation sample, and SDL3/Metal smoke tests exercise this path. `RenderScene::set_emissive` and `set_bloom` expose bounded HDR emission and bloom controls. Snapshot-created static objects now share one Wicked `MeshComponent` per mesh/material pair with instance-local transforms and reference-counted release; the SDL3/Metal regression verifies two-object sharing, independent transforms after replacement, and pair counts transitioning 2 -> 5 -> 4 -> 0. Its 200-instance stress transaction uses one shared pair and records 202 native API calls (begin, 200 staged rows, commit); updating one instance leaves another at its own position, and clear restores the native instance baseline. Rollback removes newly created unreferenced pairs. Evidence: [`docs/validation/snapshot-authored-assets.md`](docs/validation/snapshot-authored-assets.md) and [`docs/validation/elisa-render-scene.md`](docs/validation/elisa-render-scene.md).
  Progress on 2026-09-21: `WorldRendering::set_tint` gives one binding a validated color and emissive multiplier. The snapshot carries it and the presenter stages it with one extra native call only for tinted rows. Commit writes it to Wicked `ObjectComponent::color` and `emissiveColor` on every row, so clearing an override resets it. In the 200-instance stress batch, one instance is tinted while its neighbors stay neutral. The shared mesh and material factors are unchanged, and the transaction makes 203 native calls (202 after reset). Direct-ABI calls reject tints outside a transaction, for unstaged rows, duplicates, and non-finite or out-of-range values. The maze player turns a warm color while carrying the key. See [`docs/validation/instance-tints.md`](docs/validation/instance-tints.md).
  Progress: `RenderResources::create`, `update_transform`, and `update_batch` now reject singular or non-finite transforms. The batch validates every row before applying any update; `test/render_resources.elisa` verifies a later invalid row leaves earlier instance state unchanged, while preserving duplicate/stale-handle checks. The focused `render_resources` executable and full `PYTHON_BIN=/opt/homebrew/bin/python3 elisascript scripts/check.elisascript` suite passed.
  Progress: the direct scene path now exposes `RenderScene::create_mesh_instance` for static cooked sources. Clones reuse the source Wicked mesh and material through per-object `meshID` and color, reject skinned, animated, morphing and cloned sources, and keep the source mesh alive until the last clone is destroyed. SDL3/Metal case group 250 covers sharing, clone lifetime, invalid colors, and clone material rejection.
  Progress: `RenderScene::play_animation_blended` now supports eased smoothstep fades, normalized phase matching, and same-clip continuation, while `set_animation_speed` retimes the active clip without restarting its phase. SDL3/Metal case group 270 covers transition weights, cadence changes, and invalid targets.
  Completed on 2026-09-21: the stress batch also despawns one of its 200 shared instances. 199 remain, the neighbors keep their positions and material, the pair stays shared, and the transaction makes 202 native calls (begin, 199 rows, one retire, commit). Skinned snapshot meshes deliberately stay per instance: Wicked stores the armature on `MeshComponent::armatureID`, so objects sharing a skinned mesh would share one pose. Their cooked geometry is still registered and loaded once. The remaining related work moves to other tasks: asynchronous loading to A04, per-instance roughness/metalness/texture overrides to R04, duration-based throughput to R20, and many independently animated characters to C02. See [`docs/validation/snapshot-authored-assets.md`](docs/validation/snapshot-authored-assets.md).
- [ ] **R03 · P1 · Cameras and viewports** — After: R01, F09, W02.
  Implement perspective/orthographic cameras, camera switching, aspect policy, render-to-texture, picking rays, and editor/game views. Done: resize, split view, offscreen targets, frustum boundaries, and asymmetric projections behave correctly at high DPI.
  Progress: `src/backend/camera.elisa` provides validated perspective/orthographic state, high-DPI viewport resizing, split-viewport pixel-to-ray conversion, TRS-aware picking rays, clip-plane policy, and conservative transformed-AABB frustum tests; `test/camera.elisa` covers both projections, resize/suspend, valid/invalid split rectangles, and near/far/side frustum boundaries. `native/camera_bridge.h` creates/removes real Wicked perspective and orthographic views, applies high-DPI dimensions, resizes projections, and switches a live `RenderPath3D` between cameras in the native gate. The project-facing Wicked `RenderScene` now switches between validated orthographic height and vertical perspective field-of-view plus near/far clipping planes; resize preserves the active mode. Its active camera can now allocate and clear a bounded offscreen render target with a throttled update interval, and the SDL3/Metal smoke renders and inspects the real target before clearing it. `RenderScene::create_camera`, `update_camera`, `activate_camera`, `activate_default_camera`, and `destroy_camera` now expose bounded generation-checked secondary views, with active-view protection and viewport restoration covered by the smoke. `RenderScene::camera_ray` now unprojects reverse-Z Wicked coordinates through the active camera and returns a right-handed Elisa ray, with perspective and orthographic origin semantics plus out-of-bounds rejection covered by `test/render_scene_camera_native.elisa`. Multi-path scheduling and native frustum scheduling remain. Evidence: [`docs/validation/elisa-render-scene.md`](docs/validation/elisa-render-scene.md) and [`docs/validation/cameras-viewports.md`](docs/validation/cameras-viewports.md).
- [ ] **R04 · P1 · PBR material workflow** — After: R02, A05, A06.
  Expose Wicked-backed base color, metallic/roughness, normals, emissive, transparency, double-sidedness, and supported material features via engine descriptors. Done: a material reference scene and authored imports validate texture channels, tangent conventions, and color handling.
  Progress: `native/pbr_material_bridge.h` now validates and applies base/emissive color, metalness, roughness, opaque/mask/blend alpha policy, double-sidedness, shadow flags, and a bounded base-color texture slot through generation-checked Wicked material handles; the native gate covers foreign-handle rejection, unload, and a cooked Basis/KTX2 resource reaching the authored goal material. Authored glTF material upload and tangent/color reference captures remain.
  Progress: `src/backend/material.elisa` provides the backend-neutral PBR descriptor and alpha/double-sided policy; `test/material.elisa` covers valid and invalid channel/factor combinations. `RenderScene::set_alpha_mode` exposes opaque, alpha-mask and alpha-blend modes, a bounded mask cutoff, and double-sided rendering through checked instance handles. The native bridge applies Wicked alpha test/blend state and keeps masked materials shadow-casting while blended materials do not cast shadows, supporting Elisa-authored chain-link cutouts without a game-side C++ shim. Authored reference captures and tangent/color validation remain.
  Progress: snapshot material registration now resolves registered stable texture IDs into Wicked base-color, normal, packed-surface, and emissive maps, and blocks texture release while a material references it. The native SDL3/Metal smoke verifies missing-texture rejection, an uploaded Wicked texture, live-unregister rejection, and ordered material/texture cleanup. Separate glTF occlusion maps now survive cooking as Wicked `OCCLUSIONMAP` resources; hand-registered materials retain the packed-surface API and broader companion-map coverage remains.
  Progress: `RenderScene::set_texture` loads project-root-confined base-color, normal, packed-surface, and emissive maps through Wicked's resource manager, applies block compression, and requests normal-map compression for the normal slot. The cooked-mesh path uploads validated tangent frames; the native SDL3/Metal smoke rejects traversal, absolute paths, and symlink escapes. Authored normal-map/color captures, full material descriptors, and companion-map cooking remain. Evidence: [`docs/validation/render-scene-textures.md`](docs/validation/render-scene-textures.md).
  Progress: `RenderScene::set_lit_material` opts instances into Wicked PBR shading with bounded roughness/metalness and shadow casting; `set_texture_uv_transform` applies a finite per-material UV scale/offset. The native SDL3/Metal smoke confirms both reach Wicked's material/object components. Authored glTF material upload and tangent/color reference captures remain.
  Progress: authored glTF materials now reach Wicked from cooked meshes, with base-color, normal, packed-surface, emissive and separate occlusion images, plus alpha masks. The cooker and sanitized native loader cover five-slot records, while the render smoke checks each slot's bound image and size, Wicked's occlusion flag, and the rendered cutout, lit and emissive strips. See [`docs/validation/cooked-material-textures.md`](docs/validation/cooked-material-textures.md). Tangent and color reference captures remain.
  Progress on 2026-09-22: hand-registered snapshot materials can now enable occlusion. `RenderScene::register_snapshot_material_asset` passes whether `Material.occlusion` is set through the new `elisa_render_scene_v1_register_snapshot_material_asset_with_occlusion`. Native registration accepts either the packed surface map or a dedicated occlusion map, and the older registration forms keep their signatures without occlusion. In the render smoke, a hand material with the painted slot's factors, images and occlusion now matches the cooked slot, and without occlusion it still conflicts. See [`docs/validation/cooked-material-textures.md`](docs/validation/cooked-material-textures.md).
- [ ] **R05 · P1 · Lights, shadows, and environment** — After: R03, R04.
  Add directional/point/spot lights, sky/environment maps, shadow settings, exposure, and feature-dependent lighting controls. Done: moving lights, shadow bias, transparent objects, and indoor/outdoor scenes have stable reference captures and explicit quality/cost settings.
  Progress: `src/backend/lighting.elisa` defines validated backend-neutral directional/point/spot descriptors (including transforms) and sun/ambient/sky/fog values. `native/lighting_bridge.h` owns generation-checked Wicked lights and realtime environment probes. Elisa applications can now pass a validated `Lighting::Environment` through `RenderScene::set_environment`; the engine normalizes the sun direction, aligns one owned directional light to it, and applies sky, ambient, and fog values to the active Wicked scene. `RenderScene::create_light`, `update_light`, and `destroy_light` now expose bounded, opaque, generation-checked directional/point/spot handles; the native SDL3/Metal probe covers invalid spot directions, point creation and updates, spot creation, cleanup, and stale-handle rejection. `RenderScene::set_sun_shadows` controls both the owned sun's shadow casting and the active render path; `RenderScene::set_tonemap` selects checked Reinhard, ACES, or Uchimura post-processing and `set_exposure` applies a bounded scene-wide multiplier. The native SDL3/Metal probe verifies on/off state in both Wicked components and inspects the live post-process settings. Authored sky assets, profile integration, shadow-bias policy, and material/light reference captures remain.
- [ ] **R06 · P1 · Debug drawing and picking** — After: R03, W02.
  Provide scoped lines, shapes, text, depth-tested overlays, entity picking, and selection outlines from Elisa data. Done: render/physics/nav bounds can be compared in one view, picking returns checked world references, and disabled debug drawing has no steady-state allocations. Progress: `src/tooling/debug_geometry.elisa` and `test/inspector.elisa` define bounded gameplay collision geometry; `native/debug_draw_bridge.h` validates and flushes bounded boxes, lines, and copied depth-tested text, `native/picking_bridge.h` maps real Wicked ray hits to generation-checked gameplay references, and `native/selection_outline_bridge.h` toggles Wicked material outlines with explicit clear. Public `RenderScene::debug_box`, `debug_line`, `debug_text`, `debug_flush`, and `debug_clear` now queue bounded commands from Elisa, enforce owner-thread and capacity rules, and flush automatically at the render-path boundary; `RenderScene::set_pick_identity`, `pick`, `select`, and `clear_selection` expose checked ray hits and real Wicked material outlines while keeping native IDs private. SDL3/Metal/Wicked groups 231 and 232 cover valid and adversarial debug, pick, miss, invalid-ray, and outline-clear cases. Editor camera tools and world-selection overlay composition remain.
- [ ] **R07 · P1 · Post-processing and quality profiles** — After: R05, F08.
  Expose supported antialiasing/upscaling, tonemapping, bloom, fog, depth effects, and render scale through validated profiles. Done: transitions resize history buffers safely; unsupported options report fallback, and profiles carry measured GPU/VRAM costs.
  Progress: `src/backend/quality.elisa` defines validated low/medium/high profiles with explicit tonemap, upscaler, bloom, AO, SSR, fog, depth, and render-scale policy; `native/postprocess_bridge.h` applies those settings to Wicked, controls scene height fog, restores probe weather, and reports unsupported FSR as a fallback. `RenderScene::set_ambient_occlusion_settings` now exposes bounded Wicked SSAO range and power controls with native validation. History-resource transition validation and measured GPU/VRAM costs remain.
  Progress: `RenderScene::set_bloom` also provides a bounded runtime toggle/threshold on the simple scene service; the Metal smoke verifies the Elisa call and rejects thresholds above the supported range. Profile integration and visual reference captures remain.
  Progress: `RenderScene::set_ambient_occlusion` and `set_fxaa` expose Wicked's SSAO and FXAA controls through the owned scene API. The SDL3/Metal smoke checks enable/disable state in Wicked and rejects malformed native flags. `RenderScene::apply_quality_profile` now carries the validated Elisa `Quality::Profile` through the public runtime, applying tonemap, render scale, bloom, FXAA, SSAO, SSR, height fog and depth effects in one call while preserving unsupported-upscaler fallback. Image references, history-resource transition validation, persistence policy beyond the game setting, and measured GPU/VRAM costs remain. Evidence: [`post-process quality validation`](docs/validation/postprocess-quality.md).
- [ ] **R08 · P1 · Animation and morph submission** — After: R02, C02.
  Submit skeleton palettes and morph weights with explicit ownership and buffering to Wicked's supported deformation paths. Done: multiple independent animated instances share source assets, bounds update correctly, and in-flight pose memory survives until device consumption.
  Progress: `native/animation_submission_bridge.h` owns generation-checked, double-buffered bone palettes and morph weights, applies them to Wicked armature/mesh components, rejects non-finite or out-of-range data, rejects a second submission while one is in flight, and keeps the submitted buffer live until explicit completion. `RenderSceneAnimationSubmission` exposes private-field `RenderScene::AnimationPose` construction, bounded matrix/weight setters, and checked submit/complete calls; skinned cooked meshes create the bridge handle lazily. The SDL3/Metal smoke drives two independent v3 one-joint cooked meshes from the same source package through the public route, advances Wicked's armature update, verifies independent bones and bounds, checks pending completion and cleanup, and the native bridge probe checks morph application and owner validation. Elisa-driven runtime scheduling remains.
- [ ] **R09 · P1 · Native particle and decal services** — After: R02, W08.
  Expose Wicked particle/decal authoring and event-driven spawning with pooling, owner attachment, time scaling, and cleanup. Done: a combat or environmental scene uses effects through Elisa; restart and owner despawn return counts and memory to baseline.
  Progress: `native/effect_bridge.h` provides bounded generation-checked emitter/decal handles, validates count/lifetime/color/range policy, updates real Wicked particle and decal components, ticks emitter simulation, and restores component counts on destruction. `native/render_scene_effects_abi.inc` and `src/runtime/render_scene_effects.elisa` expose those controls through private descriptor fields and checked scalar handles; the SDL3/Metal smoke verifies actual Wicked component fields, malformed-input rejection, and stale-handle cleanup. `src/runtime/world_effects.elisa` now provides a bounded owner table with scoped-loop updates, transform-following, per-owner emitter time scale, and despawn cleanup; the native smoke moves an attached World entity, verifies the Wicked transform, and verifies cleanup returns the binding count to zero. Elisa event spawning, pooled authored effect assets, and a rendered combat or environmental example remain. Separately, `RenderScene` exposes up to 1,024 checked `ElectricArcHandle`s backed by Wicked trail renderers for endpoint-pinned additive core/halo ribbons and short branches. Native smoke covers invalid and degenerate input, both Elisa batch and native slot capacity, visibility, rendered-image change, destruction, and stale handles. Wicked trails now have a per-trail depth-test switch (enabled by default); the electric-arc service disables depth testing so strands stay visible over fence geometry. A live Amazing Labyrinth capture confirms the effect in an overhead view; authored socket alignment and matching every module curve remain open. Wicked's runtime trail queue submits these effects independently of its debug-draw setting (commit `c1c9300`), and commit `b7dcda7` adds the per-trail depth option. See [`docs/validation/render-effects.md`](docs/validation/render-effects.md) and [`docs/validation/elisa-render-scene.md`](docs/validation/elisa-render-scene.md).
  Progress on 2026-09-21: `RenderScene::set_electric_arc_depth_test` lets world-space arcs opt into hardware occlusion while preserving the overlay default for existing arcs. The native-only SDL3/Metal probe verifies halo, core, and branch trails retain the selected depth mode through endpoint updates; it also checks stale-handle rejection. Authored socket attachment and reference-image verification remain.
- [ ] **R10 · P2 · Visibility, batching, and LOD** — After: R02, A07.
  Connect spatial bounds, culling, instance batches, distance/screen-error LOD, and supported occlusion paths. Done: a measured large scene improves frame cost without missing newly visible objects, breaking animated bounds, or relying on warmed probe-only query state.
  Progress: `native/visibility_lod_bridge.h` now applies bounded draw-distance, LOD-bias, layer, renderable, and occlusion-culling policy to generation-checked Wicked objects; `RenderSceneVisibility` exposes that policy through a private-field constructor and the public generation-checked `RenderScene::InstanceHandle`. The SDL3/Metal render smoke verifies invalid-value rejection, Wicked object state, renderability, and the active occlusion switch. Elisa screen-error selection exists in `AssetLod`; measured large-scene batching, runtime screen-error selection, and animated-bounds coverage remain.
- [ ] **R11 · P2 · Terrain and vegetation rendering** — After: W05, A07, R10.
  Add chunked heightfield terrain, material layers, collision/nav alignment, and instanced vegetation with quality limits. Done: a traversable streamed landscape has stable seams, bounded residency, consistent picking, and measured overdraw.
- [ ] **R12 · P2 · Advanced lighting paths** — After: R05, R07, F08.
  Expose selected Wicked GI/reflection/ray-tracing features only after checking the pinned backend and device support. Done: a representative lighting scene validates enabled and fallback paths with quality/performance evidence; missing hardware remains explicitly unverified.
- [ ] **R13 · P1 · Shader pipeline and warm-up** — After: F03, R04.
  Package versioned shaders, permutations, compilation diagnostics, and pipeline-cache keys; support offline preparation where the backend permits it. Done: clean-machine startup uses packaged inputs, cache invalidation is correct, and cold/warm frame hitches are measured separately. Progress on 2026-09-22: configured shader roots are validated before Wicked initialization. The native ABI rejects missing, incomplete, non-directory, and overlong roots with `ELISA_APPLICATION_SHADER_PATH_INVALID`, and Elisa maps that result to `Application::InitializeStatus.ShaderPathInvalid`; unset or empty configuration preserves Wicked's default path. When the launcher supplies `ELISA_ENGINE_SHADER_MANIFEST`, the native boundary also rejects missing, outside-root, oversized, or malformed manifests with `ELISA_APPLICATION_SHADER_MANIFEST_INVALID`, mapped to `Application::InitializeStatus.ShaderManifestInvalid`. The SDL3/Metal native application smoke proves both rejections and still passes valid startup and failure cleanup. The macOS packager writes a deterministic `elisa.shader-manifest.json` containing a schema version, sorted compiled-binary paths, byte sizes, SHA-256 digests, and a content fingerprint; the relocated launcher exports its path. Focused tests prove relocation and fingerprint invalidation. Runtime digest verification, permutation selection, offline preparation, pipeline-cache keys, and cold/warm measurements remain ([`docs/validation/packaged-loading.md`](docs/validation/packaged-loading.md)).
- [ ] **R14 · P2 · Device failure and rendering recovery** — After: F05, F06, R13.
  Define device/swapchain failure reporting and recover or exit cleanly using authoritative asset/world state. Done: injected upload/resize/device failures release resources safely; recovery, where supported, reconstructs the scene without duplicating gameplay entities.
- [ ] **R15 · P1 · Render graph and transient targets** — After: F06, R03, R13.
  Let Elisa define ordered render passes, resource reads/writes, transient targets, and explicit dependencies over Wicked's supported paths. Done: a multi-pass authored scene rejects cycles and read/write hazards, reuses transient memory safely, and survives resize, suspension, and pass failure with reference captures.
- [ ] **R16 · P2 · GPU compute and indirect workloads** — After: F08, R13, R15.
  Expose only compute and indirect-draw operations supported by the pinned Wicked backend, with bounded dispatch descriptors, resource ownership, and completion tracking. Done: one real workload (such as culling or particle simulation) has a CPU fallback, capability negotiation, output validation, and measured cost on supported hardware.
- [ ] **R17 · P1 · Asynchronous GPU readback and capture** — After: F06, F10, R03, R15.
  Add a bounded staging/readback queue for screenshots, editor thumbnails, validation images, and GPU query results without blocking ordinary frames. Done: fence completion, cancellation, resize, queue saturation, and device failure are tested; captures identify their frame and dimensions and match a stable reference.
  Progress: the application ABI now exposes validated `ELISA_*` environment reads and an owner-thread `Application::save_screenshot` that waits for the presented Wicked back buffer and writes an RGBA PNG. The SDL3/Metal application smoke covers missing/malformed environment fallbacks, empty-path rejection, post-frame capture, and PNG signature validation. A bounded asynchronous readback queue, cancellation/resize/device-loss coverage, and stable reference comparison remain.
- [ ] **R18 · P2 · Multiple native windows and render surfaces** — After: F09, R03, R14.
  Own SDL3 windows and Wicked swapchains as independent generation-checked surfaces, each with its own resize, DPI, suspension, and close lifecycle. Done: an editor and a detached game view render simultaneously, and destroying either surface leaves the other and the game world usable.
- [ ] **R19 · P2 · Texture residency and mip streaming** — After: A04, A06, F08, R15.
  Connect cooked mip chains and queried format support to bounded asynchronous Wicked uploads, residency budgets, and eviction policy. Done: camera demand streams detail in and out without invalidating materials, exceeding configured memory, or stalling the render thread; unsupported formats use explicit tested fallbacks.
- [ ] **R20 · P1 · Render diagnostics and GPU budgets** — After: R02, R15.
  Report per-pass CPU/GPU timing, draw and upload counts, resource bytes, and frame markers through the engine's Tracy integration. Done: a representative scene produces reproducible warm/cold frame reports and regression thresholds, with unavailable GPU timings labeled rather than fabricated.

## P — physics and collision

- [ ] **P01 · P0 · Jolt ownership and stepping boundary** — After: F04, F07, R01.
  Decide from pinned source whether to expose Wicked's Jolt world or a separately controlled service; link one compatible Jolt build and disable competing simulation for managed bodies. Done: a fixed-step test proves each body advances exactly once and render updates do not secretly step it.
  Progress: `src/physics/policy.elisa` now owns a monotonic `StepClock` that rejects overlapping and non-contiguous commits, and `test/physics_policy.elisa` covers duplicate/skip prevention. `native/physics_body_bridge.h` exposes a bounded fixed-tick scene step and the native gate proves a managed dynamic body advances across exactly one authoritative boundary per tick, a kinematic target is published through Wicked's scene-transform owner path, and a dynamic body remains fixed while sleeping before gravity resumes after wake. The generation-checked physics service accepts finite, normalized kinematic targets and explicit dynamic-body sleep/wake requests for both direct worlds and session-owned services; the SDL3/Metal application smoke verifies target motion and dynamic-target rejection against the real Jolt scene. Independent 30 Hz and 120 Hz interpolation sampling now preserves the same committed fixed pose; integrated fixed-simulation/render-rate capture and competing-simulation rejection remain.
- [ ] **P02 · P1 · Body and shape lifecycle** — After: P01, F06.
  Expose static/dynamic/kinematic bodies, reusable primitive/mesh/compound shapes, mass properties, layers, and motion authority. Done: body/shape sharing, capacity failure, create/destroy, and world unload preserve identity and ownership without exposing Jolt IDs publicly.
  Progress: `src/physics/bodies.elisa` adds bounded generation-checked shapes and bodies, shared-shape lifetime protection, static/kinematic/dynamic authority, mass/layer validation, and unload counts; `test/physics_bodies.elisa` and `native/physics_body_bridge.h` are gated. Wicked now constructs typed static/kinematic/dynamic rigid-body components behind owner/generation-checked handles and proves stale/foreign rejection plus unload baseline; compound mesh cooking and broadphase layers remain.
- [ ] **P03 · P1 · Collision queries and events** — After: P02, W08.
  Implement raycasts, overlaps, shape casts, filters, triggers, and contact event queues with deterministic delivery policy. Done: nearest/all-hit queries, destroyed participants, overflow, and callback thread handoff are covered by real collision scenes and negative tests.
  Progress: `native/physics_query_bridge.h` provides generation-checked native ray nearest/all-hit, bounded sphere/capsule casts, nearest/all-hit sphere/capsule overlap queries, and a fixed-capacity worker-to-owner contact queue with overflow reporting, pair normalization, deterministic ordering, and reentrant/non-owner drain rejection. `native/physics_contact_bridge.h` drives the queue from real Jolt `Added`, `Persisted`, and cached `Removed` callbacks; the pinned Wicked adapter now exposes a sensor flag on rigid bodies and clears the listener before scene teardown. `src/physics/runtime.elisa` exposes bounded `ContactEvent`, `ContactBuffer`, sensor-capable `BodyDesc`, and public nearest/all-hit ray APIs backed by a fixed 16-hit `RayHitBuffer`; `src/physics/queries.elisa` now exposes `ShapeHit` and fixed 16-hit `ShapeHitBuffer` wrappers for sphere/capsule casts and nearest/all-hit overlaps. The flat C ABI keeps ray and shape aggregates in explicit fixed-layout buffers, while `RuntimeServices` routes all six shape forms through the affine physics session. Managed physics boxes are scaled Wicked cube entities as well as Jolt bodies, keeping scene shape queries and physics ray queries on one entity. All-hit results preserve distance order and include the nearest physics-body result when the scene query does not already contain it. The native application smoke verifies public nearest/all-hit rays, sphere/capsule casts and overlaps, invalid-input rejection and ordering, non-trigger and trigger enter events, sensor removal, sequence data, and zero overflow; the session probe covers all six shape routes on a clean miss. Finite-input validation, caller layer/filter masks, unique-entity result storage, destroyed-participant rejection, stale-token rejection, real contact delivery, public Elisa delivery, and participant removal are covered by the native gate. A gameplay sample and broader query/filter integration remain before P03 can be marked complete.
- [ ] **P04 · P1 · Fixed-step integration and interpolation** — After: P02, W02.
  Connect engine clock, substeps, kinematic targets, simulation output, and interpolated render poses with one writer per transform. Done: varied render rates produce equivalent fixed-tick results; pause, teleport, sleeping, and hitch limits behave as documented.
  Progress: `src/physics/interpolation.elisa` adds monotonic fixed-step pose publication, bounded alpha sampling, and one-sample teleport bypass; `src/world/hierarchy.elisa` keeps authoritative world poses separate from interpolation snapshots; and `src/runtime/world_rendering.elisa` now extracts render snapshots from those committed hierarchy samples without writing `World`. `test/physics_interpolation.elisa`, `test/hierarchy.elisa`, and `test/world_rendering.elisa` cover fractional samples, duplicate ticks, teleports, hierarchy publication, and a midpoint render sample while the authoritative pose stays at the latest tick. `test/clock.elisa` now drives separate 30 Hz and 120 Hz presentation loops over equal elapsed time, publishes every due fixed pose, and compares the committed tick, remainder, and sampled render pose. Portable and native interpolation probes sample identical histories at both cadences without changing the committed tick; the real-Jolt body gate verifies kinematic target publication plus sleep/wake stability. Integrated native scene capture and competing-simulation rejection remain.
- [ ] **P05 · P1 · Character controller** — After: P03, P04, I01.
  Implement supported Jolt character movement with slopes, steps, grounding, jump, crouch, moving platforms, and collision layers. Done: an interactive obstacle course has repeatable tests for corners, low ceilings, platform velocity, and frame-rate independence.
- [ ] **P06 · P1 · Constraints and interactable objects** — After: P02, P03.
  Add selected joints, limits, motors, break thresholds, sensors, and grab interactions through data-driven descriptors. Done: doors, lifts, and a jointed object run in an authored scene; deleting either endpoint safely invalidates dependent constraints.
- [ ] **P07 · P2 · Ragdoll and animated body blending** — After: P06, C04, R08.
  Cook skeleton-to-body mappings and controlled animation/ragdoll transitions with explicit ownership handoff. Done: impact, recovery, partial ragdoll, and despawn do not double-write transforms or retain stale skeleton/body references.
- [ ] **P08 · P2 · Robust physics settings and diagnostics** — After: P04, R06.
  Expose CCD, solver budgets, broadphase/layer diagnostics, material friction/restitution, and measured stability presets. Done: thin-wall projectiles, stacked bodies, fast movers, and large timesteps have reproducible outcomes and regression budgets.
- [ ] **P09 · P2 · Vehicles** — After: P05, P06.
  Wrap a supported Jolt vehicle path with suspension, wheel queries, drivetrain inputs, and render telemetry. Done: a drivable test track covers slopes, airborne wheels, collisions, reset, and clean unload; game policy remains Elisa-owned.
- [ ] **P10 · P2 · Box2D service and 2D sample** — After: F04, F06, W04, I01.
  Implement a genuine 2D physics service with independent units, shapes, bodies, contacts, queries, and joints. Done: a small authored 2D game uses it through the shared service model, with no accidental Jolt ownership or undocumented 2D/3D transform conversion.
- [ ] **P11 · P3 · Soft bodies and buoyancy** — After: P02, P08, R08.
  Expose supported soft-body and fluid-interaction capabilities behind separate feature flags and budgets. Done: one representative deformable and floating-body scene demonstrates render synchronization, stability limits, and lifecycle correctness rather than only a settings struct.
- [ ] **P12 · P2 · Large-world simulation strategy** — After: W05, P04, R11.
  Define double-precision simulation or origin rebasing based on actual library support; update rendering, audio, nav, and persistence conversions together. Done: travel far from origin preserves contact/picking precision and does not create a visible jump or corrupt saved coordinates.

## C — animation and character behavior

- [ ] **C01 · P1 · Skeleton and animation asset contract** — After: A05, F07.
  Define joint identity/order, rest and inverse-bind poses, clip tracks, events, units, and rig compatibility in cooked assets. Done: invalid parents, duplicate joints, missing tracks, and incompatible clips fail before runtime sampling.
  Progress: `src/animation/assets.elisa` validates joint IDs and parent order, metre-space rest transforms, inverse-bind matrices against the evaluated rest pose, complete clip tracks, event ordering, time bounds, and rig compatibility; the shared animation gate covers valid sampling and malformed contracts. `src/animation/limits.elisa` sets the current 64-joint bound for the pose, sampler, FBX cooker, and Wicked bridge. `Sampler::ClipData` now uses one bounded dynamic key buffer (up to 262,144 keys per clip), requires tracks in ascending joint order, and samples by binary search without per-sample allocation. This remains an in-memory contract prototype, not a cooked animation format; serialization/import and production ozz integration remain open.
- [ ] **C02 · P1 · ozz runtime animation service** — After: C01, F06.
  Promote `native/ozz_probe.h` into reusable clip/sampling-context/pose services with explicit scratch and output storage. Done: many characters sample independent clips/times without allocation per tick; the public Elisa API drives real rendered motion.
  Progress: `RenderScene::create_mesh` loads bounded sampled clip data and keeps playback state per skinned instance. Public Elisa play/stop/advance APIs drive Wicked joint transforms, and the supplied game verifies the rendered Walking clip; clip sampling currently uses the cooker's fixed-rate tracks. Production ozz contexts, allocation-free bulk sampling, authored two-clip cross-fades, and independent-instance stress tests remain.
- [ ] **C03 · P1 · Animation graph** — After: C02, W08.
  Build Elisa-owned state machines, transitions, blend trees, parameters, and interrupt rules over library sampling. Done: an authored locomotion/action graph blends correctly across transition interruptions and reports invalid graph cycles or missing nodes.
- [ ] **C04 · P1 · Layers, masks, additive motion, root motion** — After: C03, P05.
  Implement per-joint masks, additive reference poses, event timing, and one root-motion authority shared with character movement. Done: upper-body actions preserve locomotion; loops, reverse playback, skipped frames, and collision-limited motion do not duplicate events or drift.
- [ ] **C05 · P1 · IK and foot placement** — After: C04, P03.
  Promote existing two-bone/aim logic and ozz facilities into bounded rig constraints with stable foot locking and ground queries. Done: a walking character handles uneven terrain without knee flips, sliding planted feet, or mismatched solver/render coordinate spaces.
- [ ] **C06 · P2 · Retargeting and sockets** — After: C01, C04.
  Add rig maps, retarget poses, proportion handling, and attachments driven by evaluated skeletons. Done: two different rigs share a clip with documented limits; a held item stays aligned during blends, teleports, and character destruction.
- [ ] **C07 · P2 · Animation compression with ACL** — After: C02, A02.
  Add an optional cooked codec path and benchmark it against existing ozz storage on real clips using positional/angular error budgets. Done: codec choice follows measured size/decode/error results, rejects incompatible data, and does not force two decoders into every build.
- [ ] **C08 · P1 · Character gameplay composition** — After: C04, C05, P05, R08.
  Provide a reusable Elisa character composition for movement, animation, camera, interactions, and lifecycle. Done: a third-person playground and the maze reuse the same services without native code deciding movement rules or animation state.
- [ ] **C09 · P2 · Character scale and update budgets** — After: C03, W07, R10.
  Add distance-based sampling rates, animation LOD, visible-pose priorities, and batched jobs while preserving event semantics. Done: a crowd benchmark reports pose cost/memory and compares throttled output to full-rate reference clips.
- [ ] **C10 · P3 · Full-body rig solver** — After: C05, C06.
  Implement an Elisa-owned constrained full-body solver only after defining a concrete multi-effector rig and convergence/error limits. Done: reach, balance, joint-limit, and conflicting-target scenes measure quality and cost against simpler IK; failure remains bounded and diagnosable.

## N — navigation and AI foundations

- [ ] **N01 · P1 · Recast bake pipeline** — After: A05, F07.
  Promote the Recast probe into deterministic agent-profile-aware navmesh cooking with area annotations and tile metadata. Done: a multi-room/stair/obstacle scene produces cached nav data, useful bake diagnostics, and debug overlays tied to source geometry. Progress: `native/navmesh_service.h` now provides bounded deterministic baking, source-generation metadata, serialized tile bytes, and failure-atomic Recast cleanup; multi-room/stair cooks, persistent cache loading, and debug overlays remain.
- [ ] **N02 · P1 · Detour runtime queries** — After: N01, A04.
  Add navmesh ownership, nearest-point, path corridor, raycast, reachability, area cost, and off-mesh link queries. Done: an agent follows real navmesh routes; partial/no-path outcomes, query capacity, stale tiles, and invalid endpoints are explicit. Progress: the reusable adapter now owns Detour mesh/query lifetimes and exercises nearest-point, routed path, raycast, filtered/no-path, invalid-input, and bounded-capacity statuses. `NavMeshTileStore` adds bounded generation-tagged publication/unload and rejects stale query handles in the native gate; multi-tile streaming, area costs, off-mesh links, and Elisa agent movement remain.
- [ ] **N03 · P1 · Agent movement and replanning** — After: N02, P05, W08.
  Keep intent and movement policy in Elisa; implement corridor following, stuck detection, path requests, and link traversal. Done: agents reach goals through doors/platform links, replan on obstruction, and cannot overwrite character/physics-owned transforms.
- [ ] **N04 · P1 · AI decision model** — After: N03, W08.
  Add typed reusable perception, blackboard/state-machine or behavior-tree primitives with bounded update work and debug state. Done: a patrol/chase/search/interact agent operates in the character playground and can be replayed and inspected from Elisa state.
- [ ] **N05 · P2 · Crowds and avoidance** — After: N03, W07.
  Integrate DetourCrowd where it fits the movement contract, with agent lifecycle and per-tick budgets. Done: converging crowds avoid obstacles and each other, recover from blocked exits, and retain bounded cost without bypassing gameplay or physics authority.
- [ ] **N06 · P2 · Dynamic and streamed navigation** — After: N02, W05.
  Integrate tile streaming and supported DetourTileCache obstacle updates with generation-tagged query results. Done: unload/rebuild during a pending path request cannot return stale routes; rebakes are bounded and agents recover when tiles become available.

## S — audio and acoustics

- [ ] **S01 · P1 · miniaudio engine service** — After: F04, F05, F06.
  Establish one native audio-device/mixer owner, disabling or isolating overlapping Wicked/FAudio playback paths. Done: reusable clip/voice/listener APIs play real sounds and handle device loss/reopen, initialization failure, and shutdown without leaked callbacks or duplicate output.
  Progress: `native/miniaudio_service.h` now owns one null-backed miniaudio device, bounded decoded clips, generation-checked voices, allocation-free callback mixing, explicit invalid-initialization cleanup, listener state, distance attenuation, device reopen, stale-handle rejection, and shutdown invalidation. Its device callback requests recovery atomically; `RuntimeServices::pump` reopens to the silent route on the owner thread, covered by the ordinary application smoke. The callback now applies a validated per-voice spatial gain and Doppler pitch (Q16 cursor, linear interpolation, bit-identical at unit pitch), and `play` resets reused voice slots to neutral spatial state; `native/miniaudio_spatial_probe.h` covers both in the sanitized boundary harness. Streaming and Wicked/FAudio playback isolation remain.
- [ ] **S02 · P1 · Streaming, buses, and voice budgets** — After: S01, A04.
  Add decoded/streamed assets, seek/loop, music/SFX/UI buses, gain ramps, priorities, virtualization, and voice limits. Done: long tracks stream within budget; cancellation, queue underrun, and voice stealing are tested without allocation or blocking IO in the audio callback.
  Progress: the bounded miniaudio service now exposes Music/SFX/UI buses, per-bus gain and voice budgets, priority-based deterministic voice stealing, and callback-side mixing with no allocation. `AudioRuntime` also provides safe empty clip/voice sentinels for optional game-owned sound state; the native application smoke verifies invalid-sentinel rejection and real fixture playback. See [`docs/validation/audio-runtime.md`](docs/validation/audio-runtime.md). Streamed assets, seek/cancellation, gain ramps, virtualization policy, and underrun tests remain.
- [ ] **S03 · P1 · Spatial audio and world attachment** — After: S01, W02, P03.
  Implement listeners, distance/cone attenuation, velocity/Doppler policy, and basic occlusion using engine transforms and query results. Done: moving sources/listeners sound consistent with scene scale, and entity destruction safely detaches active voices.
  Progress: `src/audio/spatial.elisa` now owns bounded entity-keyed source attachment, listener/source velocity, distance and cone gain, occlusion, Doppler ratio, and detach/update validation; `test/audio_spatial.elisa` covers the portable policy. `native/miniaudio_service.h` now consumes source velocity and occlusion and exposes a bounded Doppler ratio in the real callback mixer, with native rejection tests. `SpatialAudio` now rejects non-finite source vectors and NaN range, cone, and occlusion values. `WorldAudio` in the public runtime bundle attaches voices to World entities, drives the listener from a tracked entity, updates sources from committed transforms, applies occlusion supplied by the caller, and detaches despawned entities, ended voices, and a lost listener without leaving stale native voices. The native application smoke covers rejected attachments, Doppler and range response of moving emitters, hitch velocity reset, occlusion validation, despawn and finished-voice detach, listener loss, and invalid time steps. Evidence: [`docs/validation/spatial-audio.md`](docs/validation/spatial-audio.md). Occlusion driven by physics queries waits on P03.
- [ ] **S04 · P2 · Steam Audio integration** — After: S03, A11.
  Add opt-in HRTF and supported geometry-based acoustic effects with baked/runtime data and explicit worker budgets. Done: an indoor/outdoor scene compares enabled/fallback paths, async scene changes, device compatibility, CPU use, and audio latency.
- [ ] **S05 · P1 · Audio gameplay events and authoring** — After: S02, W08.
  Define sound event assets, weighted variants, concurrency groups, mixer snapshots, and animation/physics triggers. Done: footfalls, impacts, ambience, and music transitions use editor-authored data and do not double-fire during replay, pause, or reload.
- [ ] **S06 · P2 · Voice capture and Opus transport** — After: S02, T01, I03.
  Add explicit user-enabled microphone capture, Opus frames, jitter buffering, optional RNNoise, and session-scoped playback. Done: two processes exercise mute, device changes, packet loss, cleanup, latency, and bandwidth; capture never starts implicitly on scene load.
- [ ] **S07 · P2 · Audio regression harness** — After: S02, S03.
  Capture offline mixes and timing telemetry for gain, clipping, channel mapping, looping, and spatial fixtures. Done: deterministic DSP assertions complement audible/manual checks and expose underruns or callback overruns in long-session runs.

## I — input, UI, accessibility, and platform interaction

- [ ] **I01 · P0 · Action input and binding maps** — After: F09.
  Extend portable input to keyboard, mouse, gamepad, analog axes, dead zones, chords, rebinding, and gameplay/UI contexts. Done: hotplug, lost focus, held/released transitions, and replayed input behave consistently in the persistent native host.
  Progress: `src/runtime/action_input.elisa` provides bounded context-aware bindings, dead zones, per-frame edges, connection cleanup, and multi-binding action aggregation. Its bounded map holds 48 bindings and 32 distinct action states, enough to retain keyboard aliases, gamepad movement, and a separate UI context without dropping controls. The 32-action bound supports game clients that model WASD and arrows as separate settings-controlled actions alongside gameplay and menu actions. Chord state is tracked per binding, so either key can arrive first and releasing one chord leaves another valid binding active. `src/runtime/action_input_runtime.elisa` drains the ordinary Application queue into portable action events and clears held state on focus loss, queue overflow, and every gamepad disconnect, even while another gamepad remains connected. The ordinary Elisa Application queue maps a supported keyboard subset and gamepad buttons/axes to stable engine codes, normalizes stick and trigger values, reports up to four open gamepads, and carries analog magnitudes at 20-bit precision. `ActionInput::last_active_device` exposes the last bound press above its dead zone, defaults to keyboard, ignores releases/noise, and falls back to keyboard on disconnect. H is now a stable Elisa/SDL keyboard code for projects that expose a help action. The default public module exports the event router. Tests cover typed binding errors, capacity, portable codes, chord order and independence, mouse/gamepad translation, disconnect cleanup, overflow reset, and deadzone transitions; `test/application_gamepad_codes.cpp` checks SDL mapping and axis/token conversion. No physical controller was connected. Gamepads currently share one logical action-device state; per-controller bindings, saved rebinding, full key coverage, mouse motion/scroll, and replay injection remain.
  Follow-up: `set_context` now clears held values and pending press/release edges so an action queued in gameplay cannot leak into a UI context. The focused Elisa fixture verifies that a press survives render frames with no fixed update, is visible on the first catch-up tick only, and keeps its held value over subsequent ticks. It also verifies that switching contexts discards an unconsumed edge. `elisac-stage1 -emit exe -o build/action-input-test test/action_input.elisa && build/action-input-test` passed.
  Progress: `test/application_native_main.elisa` now calls `ActionInputRuntime::begin_frame` in the ordinary SDL3/Metal application smoke. The focused action-input executable, native SDL mapping/token test, application lifecycle smoke, and full `scripts/check.elisascript` suite passed after this integration. See [`docs/validation/action-input.md`](docs/validation/action-input.md).
- [ ] **I02 · P1 · UI rendering and interaction** — After: R03, I01, A04.
  Connect existing Elisa UI/layout/style models and suitable Elisa UI ecosystem modules to a persistent Wicked draw path, with clipping, scrolling, focus, and hit testing. Done: menus and HUD remain visible and interactive during gameplay rather than being removed before capture.
  Progress: commit `10dd6cd` adds a public `RenderScene` screen-space text API with checked handles, text and signed-integer updates, visibility, and destruction. The follow-up position setter lets Elisa relayout existing labels on resize without rebuilding the scene. Its Wicked smoke checks invalid input, stale-handle errors, bounded position updates, scene-reset invalidation, and actual overlay pixels. Validation: [`docs/validation/overlay-text.md`](docs/validation/overlay-text.md). Buttons, layout integration, clipping, scaling, focus/navigation, hit testing, and shaped font assets remain open.
  Capacity update: the screen-space text table now holds 64 generation-checked labels so a game can keep HUD, menu and help text live together. The RenderScene native smoke fills the table, checks capacity rejection and releases every handle. Full UI layout, clipping, focus/navigation and shaped fonts remain open.
  Progress on 2026-09-22: `RenderScene::create_overlay_image` adds bounded project-relative textured quads with generation-checked handles, normalized atlas UVs, position/size/color/visibility updates, and deterministic destruction. The SDL3/Metal smoke loads a real PNG atlas through Wicked, selects its blue swatch, verifies the rendered pixel, hide/show, invalid paths and rectangles, and stale-handle rejection. Validation: [`docs/validation/overlay-image.md`](docs/validation/overlay-image.md). Clipping, layout, shaping, and retained UI composition remain open.
  Progress on 2026-09-23: `src/runtime/ui_renderer.elisa` retains RenderScene panel/text handles and maps `Ui::Menu`, `UiLayout`, and `UiStyle` into a visible, scrollable menu; menu actions stay separate from static labels. The focused SDL3/Metal/Wicked smoke passed focus skipping, scroll/re-layout, disabled-row styling, frame pumping, and cleanup; the full ElisaScript gate also passed. The broader render-scene app still aborts with exit 134 after Wicked creates its first 256 MiB GPU buffer, before its assertions run. See [`docs/validation/ui-menu-renderer.md`](docs/validation/ui-menu-renderer.md). Pixel-diff assertions, clipping, DPI scaling, shaping, and live/dynamic label ownership remain open.
  Follow-up on 2026-09-23: `RenderScene::measure_overlay_text` exposes Wicked SpriteFont width/height in logical pixels and reports when dynamic glyph-atlas metrics are ready. `UiText::Run` now retains optional word labels, validates wrap results against their source run, and safely rejects layouts that exceed the 16-line bound. `UiRenderer::sync_wrapped` applies the Elisa line-break model to retained text handles; focused SDL3/Metal tests exercise multi-line layout, shrink-to-empty visibility, font-size scaling, and atlas readiness. `test/editor.elisa`, the full `scripts/check.elisascript` gate (with Homebrew Python 3.14), module/source policies, the dependency manifest and focused native UI smoke pass. Tokenization, selectable fonts, clipping, DPI scaling, shaped/Unicode text, and pointer interaction remain open. Validation: [`docs/validation/ui-menu-renderer.md`](docs/validation/ui-menu-renderer.md).
- [ ] **I03 · P1 · Platform services and settings** — After: F09, W06.
  Add user-data paths, clipboard, display/audio-device selection, graphics/input settings, and platform permission results. Done: saved settings apply safely on restart and reset/fallback behavior works when devices or displays disappear.
  Progress: the public user-data blob now supports up to 16 signed 64-bit fields
  while retaining the version-1 file format and accepting existing smaller
  records. The C ABI exports `ELISA_USER_DATA_MAX_FIELDS`, Elisa `Save` and
  `UserData` share the same bound, and native/application smoke coverage proves
  maximum-size round-trips, atomic replacement, corruption rejection and
  staged-read preservation. Display/audio-device selection and richer settings
  remain open.
- [ ] **I04 · P1 · Unicode text and font assets** — After: I02, A04.
  Integrate FreeType/HarfBuzz with fallback fonts, glyph atlases, shaping caches, and justified ICU segmentation/bidi support. Done: mixed-direction, ligature, combining-mark, and fallback-font fixtures render and hit-test correctly without treating code units as glyph indices.
- [ ] **I05 · P1 · Editable text and IME** — After: I04, I01.
  Implement selection, caret movement, composition, clipboard operations, undo, and password-field policy using SDL3 text events. Done: multilingual IME composition and grapheme-aware deletion work at multiple DPI scales without leaking gameplay input.
- [ ] **I06 · P1 · Accessible game UI** — After: I02, I03.
  Add complete keyboard/controller navigation, scalable text, contrast themes, remapping, captions, reduced-motion settings, and semantic control metadata. Done: the sample can be configured and played without a mouse; platform accessibility bridges are reported separately from visual accessibility options.
- [ ] **I07 · P1 · UI data binding and reusable widgets** — After: I02, I05.
  Add typed model bindings, lists/tree views, sliders, inspectors, validation/error states, and virtualized large collections. Done: editor and game settings reuse the widgets; removal/reordering cannot leave stale focus or callbacks, and large lists remain bounded.
- [ ] **I08 · P2 · Vector graphics** — After: I02, A04.
  Integrate ThorVG for a concrete SVG/Lottie UI asset path with scale, animation, clipping, and cache lifetime policy. Done: a real HUD/editor panel consumes vector assets with validated rendering cost and graceful unsupported-format diagnostics.
- [ ] **I09 · P2 · Touch and haptics** — After: I01, I03.
  Add multi-pointer actions, gestures, controller vibration, and capability-based haptic output through SDL3. Done: touch/controller-specific examples handle cancellation and disconnect; unsupported devices preserve gameplay and disclose unavailable feedback.
- [ ] **I10 · P2 · Localization pipeline** — After: I04, E04.
  Define stable message keys, plural/locale formatting, extraction, translations, and live language switching. Done: a long-text and right-to-left locale exercise layout, missing-key fallbacks, and packaged locale data without rebuilding gameplay code.

## T — networking and online runtime

- [ ] **T01 · P1 · GameNetworkingSockets session service** — After: F04, F05, W08.
  Promote the loopback probe into server/client connection lifecycle, reliable/unreliable channels, backpressure, timeouts, and structured errors. Done: two independent processes connect, exchange gameplay data, disconnect/reconnect, and shut down cleanly; P2P/relay support is claimed only when actually configured and tested.
- [ ] **T02 · P2 · Versioned multiplayer protocol** — After: T01, W04.
  Extend the fixed demo frame into negotiated schemas, entity spawn/despawn, RPC/event limits, ownership, and authorization checks. Done: incompatible peers, malformed/oversized traffic, duplicate messages, and unauthorized state changes fail safely before world mutation.
- [ ] **T03 · P2 · Replication and interest management** — After: T02, W05.
  Implement subscriptions, relevance, baselines, deltas, acknowledgments, and per-client bandwidth budgets from Elisa world data. Done: separate clients receive only relevant entities; loss and late joining recover full state without stale references or unbounded queues.
- [ ] **T04 · P2 · Prediction, reconciliation, lag handling** — After: T03, P04, W09.
  Extend existing prediction/interpolation/recovery models to the real transport and character loop, defining solver determinism limits. Done: a two-process game tolerates configured delay/loss/reordering with measured correction error and bounded input/history buffers.
- [ ] **T05 · P2 · Dedicated headless server** — After: T02, P01, W07.
  Build a server target that links required simulation services without graphics, audio devices, or a display server. Done: remote clients play an authored level; tick overrun, shutdown, persistence, and metrics work on a clean machine.
- [ ] **T06 · P2 · Multiplayer test laboratory** — After: T03, T04, T05.
  Add deterministic transport fault injection, reconnect/late-join tests, traffic recording, protocol fuzzing, and long multi-client sessions. Done: failures include seed/trace/build identities and compare server/client state against explicit convergence bounds.
- [ ] **T07 · P2 · HTTP and downloadable content** — After: A03, A04, T01.
  Add cancellable libcurl-backed requests, bounded responses, progress, integrity-checked downloads, and atomic package installation. Done: timeout, resume, corrupted content, and interrupted updates preserve the last usable package; service credentials never enter logs or bundles.
- [ ] **T08 · P2 · Network operations and session UI** — After: T01, I07, Q02.
  Expose connection status, host/join workflow, errors, player roster, diagnostics, and configurable service discovery. Done: a packaged two-player sample is usable without editing source paths; hosted matchmaking/relay integrations remain explicit adapters with external prerequisites.

## E — editor, gameplay authoring, and iteration

- [ ] **E01 · P1 · Native editor shell** — After: F02, R03, I02.
  Build a persistent editor host with scene viewport, hierarchy, inspector, asset browser, console, and play controls over existing Elisa editor models. Done: a developer opens a project and runs gameplay without a Godot editor dependency or native hardcoded content.
- [ ] **E02 · P1 · Entity selection and transform tools** — After: E01, R06, W02.
  Add checked selection, multi-selection, translate/rotate/scale gizmos, snapping, local/world modes, and hierarchy reparenting. Done: edits are accurate under nested/nonuniform transforms and are reversible without leaking runtime IDs into saved scenes.
- [ ] **E03 · P1 · Transactional undo/redo** — After: E02, W04.
  Generalize existing field undo to create/delete/reparent, prefab overrides, and multi-object operations with bounded history. Done: undo after asset reload or selection deletion behaves predictably; failed commands leave the document unchanged.
- [ ] **E04 · P1 · Project and scene workflow** — After: E03, A02, W06.
  Implement project creation/open, scene save/load, autosave/recovery, recent projects, dirty indicators, and external-change handling. Done: a new project can author and reopen a scene with stable references after renames, crashes, and interrupted writes.
  Progress: commit `34ca176` adds runner support for `elisa.project.json` main source, output, and validated native application defaults; CLI path overrides remain available. Fake-tool tests cover resolution, precedence, paths with spaces, setting delivery, and invalid values; the SDL3/Wicked native smoke consumes manifest dimensions through `Application::default_config()`. Evidence: [`docs/validation/project-settings.md`](docs/validation/project-settings.md). Project creation, scene persistence/recovery, editor integration, and package migration remain.
- [ ] **E05 · P1 · Asset browser and import UI** — After: E01, A02, A10, I07.
  Add thumbnail previews, type filtering, import settings, dependencies, diagnostics, drag/drop, and reimport progress. Done: an artist imports a model/texture/audio clip and places it in a scene; missing/broken assets remain repairable instead of disappearing silently.
- [ ] **E06 · P1 · Play-in-editor isolation** — After: E04, F05, W04.
  Clone/remap an authored world for play, support pause/step/restart, and define explicit apply-back rules. Done: repeated play sessions neither change the saved world nor leak native resources; the user can inspect runtime state and return to editing safely.
- [ ] **E07 · P1 · Material and environment editors** — After: E05, R04, R05.
  Add live material previews, texture-slot editing, lighting/environment controls, and quality settings using runtime descriptors. Done: saving a material updates all intended instances through generation-safe reload; invalid edits preserve the last valid render state.
- [ ] **E08 · P2 · Animation and rig authoring** — After: E05, C03, C05.
  Add clip preview, transition/blend editing, event tracks, masks, IK targets, and pose diagnostics. Done: an artist builds and saves a locomotion graph that the standalone runtime evaluates identically without editor-only data dependencies.
- [ ] **E09 · P1 · Physics/nav/audio authoring** — After: E05, P06, N02, S05.
  Expose colliders/joints/layers, nav areas/links/bake controls, listeners/sources, and sound events with matching debug visualization. Done: a level assembled through the editor is playable with collisions, navigation, and audio in a packaged native build.
- [ ] **E10 · P2 · Safe code iteration and extension APIs** — After: E06, W08, F05.
  First provide rebuild-and-restart preserving authored state; add native code reload only after proving quiescence, callback drain, ABI compatibility, and state migration. Done: old code cannot remain callable after replacement; incompatible changes fall back to a controlled restart with clear diagnostics.

## Q — performance, testing, packaging, and platforms

- [ ] **Q01 · P0 · Representative native regression scenes** — After: F10, R01.
  Keep the maze and add independent fixtures for resource churn, many instances, lit materials, and interactive lifecycle behavior. Done: native tests exercise real services with meaningful assertions and structured failure artifacts; scene counts or nonblank pixels alone are insufficient.
- [ ] **Q02 · P1 · Standalone Release packaging** — After: F03, F05, A03, R13.
  Package executable, runtime libraries, shaders, assets, settings defaults, and notices with relocatable paths. Done: a Release game runs offline outside the repository on a clean target machine, without Homebrew/source-tree paths or runtime downloading build dependencies.
  Progress: `scripts/package_macos_app.py` accepts an optional manifest `package.resources` allowlist, rejects traversal, litter, missing, and symlinked resources, stages only declared runtime files, filters repository/editor metadata, and exports a relocated shader path from the launcher. Focused tests cover declared and fallback staging, relocation, plist metadata, and invalid manifests; full clean-target packaging and dependency notices remain.
  Wicked's `MakePathRelative` now uses the non-throwing `std::filesystem::relative`, because `ShaderInterop.h` runs it from a static initializer and an unreadable build-machine path aborted a sandboxed relocated bundle before `main`. The labyrinth's relocated bundle then ran from a path with spaces once the render scene instance table grew to 512 (the full maze needs 258). The packager now walks `otool -L` transitively, copies every non-system dynamic library (nine Homebrew libraries for the labyrinth, from SDL3 down to pcre2) into `Contents/Frameworks`, rewrites the load commands to `@rpath` with a loader-relative run path and ad-hoc re-signs each file; a clang-built fixture proves a two-level closure runs after its source prefix is deleted, and the labyrinth bundle runs under a sandbox that denies both the checkouts and `/opt/homebrew`. Settings defaults, release signing and a clean-machine run remain.
- [ ] **Q03 · P1 · Native CI and toolchain bootstrap** — After: F03, F10, Q01.
  Extend current CI with pinned compiler/prover/ElisaScript provisioning, native contract tests, and artifact retention; separate GPU workstation jobs from hosted headless jobs. Done: missing tools fail visibly, caches are identity-keyed, and every claimed platform has its own execution evidence.
- [ ] **Q04 · P1 · Crash and actionable diagnostics** — After: F05, Q02.
  Add bounded structured logs, source/build IDs, symbolized crash artifacts, reproducible launch arguments, and optional explicitly configured Sentry Native integration. Done: a packaged induced failure yields usable local diagnostics; telemetry requires configuration and excludes private project/user content by default.
- [ ] **Q05 · P2 · Tracy and engine performance budgets** — After: W07, R10, C09.
  Instrument simulation, jobs, IO, uploads, GPU frames, audio, locks, and allocation lifetimes; establish scene/hardware-specific regression budgets. Done: optimized-build traces locate bottlenecks, include cold/warm runs, and distinguish profiling overhead from engine cost.
- [ ] **Q06 · P2 · Soak, stress, and sanitizer coverage** — After: F05, W05, A10, T06.
  Automate repeated load/unload, reload, connect/disconnect, resize, and input stress with fault injection. Done: multi-hour runs have bounded resources and usable reproduction traces; ASan/UBSan and supported thread/GPU validation paths cover real adapters, with unsupported checks disclosed.
- [ ] **Q07 · P1 · Second authored game and SDK example** — After: E06, P05, C08, S05, Q02.
  Build a small third-person exploration/combat game using public APIs and editor-authored assets rather than maze-specific shims. Done: documented new-project-to-package steps work; any sample-only native gameplay logic is moved into reusable Elisa services or the game's Elisa code.
- [ ] **Q08 · P2 · Windows and Linux native targets** — After: Q02, Q03, F09.
  Implement/test platform shims, dependency builds, SDL3 windows, shader compilation, and the Wicked graphics path appropriate to each target. Done: the same authored sample passes startup, gameplay, input, rendering, resource teardown, and packaging on actual Windows and Linux environments.
- [ ] **Q09 · P2 · Distribution readiness** — After: Q02, Q08.
  Generate dependency/license notices and a component inventory, review redistribution requirements, and prepare platform signing/install/update workflows. Done: artifacts are reproducible and attributable; the project license and production signing credentials remain explicit owner decisions, never invented by an agent.

## X — specialist expansion with concrete consumers

- [ ] **X01 · P2 · Procedural world generation** — After: R11, W05, A02.
  Integrate FastNoise2 through deterministic versioned generator assets for terrain/biomes/caves with bounded jobs and cached output. Done: a seeded traversable world regenerates reproducibly, streams within budget, and shares geometry with collision and navigation.
- [ ] **X02 · P3 · XR runtime** — After: F08, R03, I09, Q02.
  Build an optional OpenXR service for session lifecycle, views, predicted poses, actions, and device loss, after confirming the chosen Wicked target's support. Done: a real headset sample validates timing, tracking-origin conventions, stereo rendering, and clean fallback when no runtime exists.
- [ ] **X03 · P3 · Cinematics and video** — After: S02, R03, E04.
  Add an Elisa timeline for cameras, animation, audio, events, and skippable sequences; integrate FFmpeg only for a concrete video/import path. Done: seeking, pause, skip, AV synchronization, resource release, and distribution requirements are demonstrated in a packaged sequence.
- [ ] **X04 · P3 · Production image and color pipeline** — After: A06, R04, E07.
  Add justified OpenImageIO/OpenColorIO offline import and color-transform stages with explicit working/display spaces. Done: HDR/EXR and reference-chart fixtures survive cook-to-display with documented precision, metadata, and renderer limitations.
- [ ] **X05 · P3 · MaterialX and USD interchange** — After: A05, E04, E07.
  Implement bounded supported subsets that translate into engine-owned scene/material assets, keeping heavy interchange tools offline where possible. Done: one real production asset round-trips supported data, and unsupported graph/schema features are diagnosed without implying full compatibility.
- [ ] **X06 · P3 · Sparse volumes** — After: X01, A04, R12.
  Add an OpenVDB/NanoVDB asset route only with a defined volume-rendering/query consumer supported by the pinned renderer or a reviewed extension. Done: a cloud/voxel example demonstrates streaming, transforms, GPU/CPU budgets, and explicit unsupported-target behavior.
- [ ] **X07 · P3 · External VFX authoring** — After: R09, E05.
  Integrate Effekseer only when a representative effect cannot be reasonably authored through the Wicked path; define lifetime and renderer submission ownership first. Done: an authored effect responds to Elisa events, reloads, packages, and tears down without a second gameplay clock.

## Validation and handoff

Use the existing commands as regression anchors:

- `elisascript scripts/check.elisascript` — shared runtime, rejection, proof, package, and Godot compatibility checks.
- `elisascript scripts/native_gate.elisascript native` — native-only startup, application, SDL3/Wicked, and library integration gate with structured evidence.
- `elisascript scripts/wicked_probe.elisascript` — focused real native graphics and library checks; requires the configured Wicked build and a suitable graphics session.
- `python3 scripts/run_boundary_sanitized.py` — untrusted native boundary checks.
- `python3 test/check_workflow.py ~/.local/bin/elisascript` — orchestration behavior only.
- `python3 scripts/check_module_hygiene.py` and `python3 scripts/check_source_length.py` — repository policies, including this plan's 600-line limit.
- When changing compiler field/reference semantics, run the owning compiler's targeted
  regressions and `test/parity/driver_acceptance_smoke.sh`; do not raise its baseline to hide regressions.

Every handoff names: completed task IDs, commits/repos, commands and outcomes, evidence
files, API changes, remaining limitations, and the next ready task. Maintain separate
statuses for implemented, integrated, tested, proved, hardware-unverified, and blocked.
No number of checked boxes substitutes for the milestone's playable and packaged result.
