# Smoother clip transitions are part of the core RenderScene API so handle
# internals remain private while all scene clients share one animation surface.
include "./render_scene.elisa"

module RenderSceneAnimationBlend:
    public:
        const API_VERSION: i32 = 1
