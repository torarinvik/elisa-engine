# Shared-mesh instance creation is part of the core RenderScene API so the
# source handle stays opaque to clients and optional module imports.
include "./render_scene.elisa"

module RenderSceneInstancing:
    public:
        const API_VERSION: i32 = 1
